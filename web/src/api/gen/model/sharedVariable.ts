/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 * OpenAPI spec version: 0.3.0
 */

export interface SharedVariable {
  /** sv_… prefixed ID. */
  id: string;
  project_id: string;
  /**
     * null at project scope; otherwise the environment this variable is narrowed to.
     * @nullable
     */
  environment_id: string | null;
  /** Empty at project scope. Present so a list needs no second lookup to render the scope. */
  environment_name: string;
  /** Matches [A-Za-z_][A-Za-z0-9_]* — the same charset as an application's own env keys. */
  key: string;
  /** How many applications reference this variable. Scope-accurate: an application whose own environment defines a shadowing row of the same key is not counted, because it does not use this one. */
  used_by_count: number;
  created_at: string;
  updated_at: string;
}
