/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 *
 * Every response — success, error and SSE stream alike — carries an `X-Request-Id` header (`components/headers/RequestId`), and every JSON error body repeats it as `trace_id`. It is the value to quote in a bug report and the key to search for in `GET /api/v1/panel/logs`. Individual responses reference the header only where a generated client benefits; it is present on all of them.
 * OpenAPI spec version: 0.3.0
 */
import type { ListAuditEventsOutcome } from './listAuditEventsOutcome.ts';

export type ListAuditEventsParams = {
/**
 * Only this team's rows. A team the caller does not belong to yields an empty page rather than 403.
 */
team_id?: string;
/**
 * Only rows scoped to this project.
 */
project_id?: string;
/**
 * One resource's whole timeline — every action ever recorded against this id, including the one that deleted it.
 */
resource_id?: string;
/**
 * An exact verb (`deploy.started`) or a whole family by prefix (`deploy` selects every `deploy.*`). The vocabulary is closed and documented in `AuditEvent.action`.
 */
action?: string;
/**
 * A user id (`usr_…`) or the email label recorded at the time, so "priya@example.com" works without resolving it to an id first.
 */
actor?: string;
/**
 * `failure` is every refused action — a wrong password, a deploy a freeze window turned away, a rejected approval.
 */
outcome?: ListAuditEventsOutcome;
/**
 * RFC 3339 lower bound on `at`. Omitted means unbounded.
 */
since?: string;
/**
 * Cursor: the id of the last event on the previous page. An id the caller may not see yields an empty page rather than restarting at the newest row.
 */
before?: string;
/**
 * @minimum 1
 * @maximum 200
 */
limit?: number;
};
