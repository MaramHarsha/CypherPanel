/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 * OpenAPI spec version: 0.3.0
 */
import type { DatabaseEngine } from './databaseEngine';
import type { DatabaseCpuLimit } from './databaseCpuLimit';
import type { DatabaseMemoryLimitMb } from './databaseMemoryLimitMb';
import type { DatabaseExposePort } from './databaseExposePort';
import type { DatabaseStatus } from './databaseStatus';
import type { DatabaseDesiredState } from './databaseDesiredState';
import type { DatabaseDesiredRevisionId } from './databaseDesiredRevisionId';

export interface Database {
  /** db_… prefixed ID. */
  id: string;
  environment_id: string;
  name: string;
  engine: DatabaseEngine;
  version: string;
  server_id: string;
  /** Fractional cores; null/0 = no limit. */
  cpu_limit?: DatabaseCpuLimit;
  memory_limit_mb?: DatabaseMemoryLimitMb;
  /** Deterministic: cypher-db-<id>. */
  volume_name: string;
  /** Host port; null = private only. */
  expose_port?: DatabaseExposePort;
  network: string;
  root_user: string;
  /** Present only on create / reset; shown once. */
  root_password?: string;
  require_password: boolean;
  status: DatabaseStatus;
  status_detail?: string;
  /** What the operator asked for, as distinct from `status` (what the agent last observed). Start/stop are guarded on this, so a client must gate those actions on `desired_state` rather than `status` — otherwise it offers Start whenever reality is still catching up with intent, and the call is rejected. */
  desired_state: DatabaseDesiredState;
  desired_revision_id?: DatabaseDesiredRevisionId;
  observed_revision_id?: string;
  created_at: string;
  updated_at: string;
}
