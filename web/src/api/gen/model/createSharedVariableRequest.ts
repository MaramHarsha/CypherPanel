/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 * OpenAPI spec version: 0.3.0
 */
import type { CreateSharedVariableRequestEnvironmentId } from './createSharedVariableRequestEnvironmentId';

export interface CreateSharedVariableRequest {
  /** [A-Za-z_][A-Za-z0-9_]* */
  key: string;
  /** Omit or send null for project scope. An environment of another project is a 400 — the foreign key alone cannot express that. */
  environment_id?: CreateSharedVariableRequestEnvironmentId;
  /**
   * The value, sealed at rest and never returned. It is stored verbatim: a nested shared reference inside it is rejected — there is no nesting.
   * @maxLength 32768
   */
  value: string;
}
