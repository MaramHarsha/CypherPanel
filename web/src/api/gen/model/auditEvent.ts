/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 *
 * Every response — success, error and SSE stream alike — carries an `X-Request-Id` header (`components/headers/RequestId`), and every JSON error body repeats it as `trace_id`. It is the value to quote in a bug report and the key to search for in `GET /api/v1/panel/logs`. Individual responses reference the header only where a generated client benefits; it is present on all of them.
 * OpenAPI spec version: 0.3.0
 */
import type { AuditActor } from './auditActor.ts';
import type { AuditEventDetail } from './auditEventDetail.ts';
import type { AuditEventOutcome } from './auditEventOutcome.ts';
import type { AuditResource } from './auditResource.ts';

export interface AuditEvent {
  /** aud_… prefixed ID. */
  id: string;
  at: string;
  /** A dotted verb from the closed vocabulary: the family before the dot, the past-tense event after it. The families are `auth`, `token`, `user`, `team`, `server`, `deploy_key`, `project`, `environment`, `application`, `database`, `deploy`, `protection`, `backup_target`, `backup_schedule`, `backup`, `shared_variable`, `notifier`, `webhook_endpoint` and `panel`. Filter on a family with `?action=deploy`. The list grows additively; a client must treat an unrecognised verb as ordinary text. */
  action: string;
  /** Failure is an outcome, not a separate verb, so "everything that was refused" is one predicate over the whole vocabulary. A refusal carries its reason in `detail.reason`. */
  outcome: AuditEventOutcome;
  actor: AuditActor;
  resource: AuditResource;
  /** The owning team at the time. EMPTY means the action was panel-level — a server, a user, the mail/DNS/TLS settings — and only panel admins may read it. */
  team_id: string;
  /** Empty when the action was not scoped to a project. */
  project_id: string;
  /** Empty when the action was not scoped to an environment. */
  environment_id: string;
  /** Structured extras: identifiers, key NAMES, counts, and the reason a refusal was refused. Never a secret VALUE — an env-var change records `{"key": "DATABASE_URL"}` and nothing else, and the audit service strips a denylist of secret-shaped key names as a second line of defence. */
  detail: AuditEventDetail;
  /** The `X-Request-Id` of the request that performed the action, so a trace id from a screenshot finds what it did as well as what it logged. */
  trace_id: string;
  /** The address the panel attributed the request to — the same value the sign-in throttle counts against. Empty for actions with no request behind them. */
  client_ip: string;
}
