/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 * OpenAPI spec version: 0.3.0
 */
import type { AppSource } from './appSource';
import type { AppBuild } from './appBuild';
import type { AppRuntime } from './appRuntime';
import type { AppRoute } from './appRoute';
import type { AppHealth } from './appHealth';
import type { AppVolume } from './appVolume';
import type { AppPort } from './appPort';
import type { CreateApplicationRequestEnvVars } from './createApplicationRequestEnvVars';

export interface CreateApplicationRequest {
  /**
   * @minLength 1
   * @maxLength 100
   */
  name: string;
  source: AppSource;
  build?: AppBuild;
  runtime: AppRuntime;
  route: AppRoute;
  health?: AppHealth;
  volumes?: AppVolume[];
  /** Raw host-port publishes (tcp/udp); route.domain may be empty for a raw service. */
  ports?: AppPort[];
  /** Plaintext values; sealed before storage, never returned. */
  env_vars?: CreateApplicationRequestEnvVars;
}
