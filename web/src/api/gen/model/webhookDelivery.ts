/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 * OpenAPI spec version: 0.3.0
 */
import type { WebhookDeliveryResourceKind } from './webhookDeliveryResourceKind';
import type { WebhookDeliveryStatus } from './webhookDeliveryStatus';
import type { WebhookDeliveryResponseStatus } from './webhookDeliveryResponseStatus';
import type { WebhookDeliveryDurationMs } from './webhookDeliveryDurationMs';
import type { WebhookDeliveryNextAttemptAt } from './webhookDeliveryNextAttemptAt';
import type { WebhookDeliveryRedeliveryOf } from './webhookDeliveryRedeliveryOf';

export interface WebhookDelivery {
  /** whd_… prefixed ID — this is the X-CypherPanel-Delivery header value. */
  id: string;
  endpoint_id: string;
  /** deploy.succeeded | deploy.failed | backup.succeeded | backup.failed | webhook.ping */
  event_type: string;
  resource_kind: WebhookDeliveryResourceKind;
  /** The resource this event describes. Deliberately not a foreign key — the log outlives what it describes. */
  resource_id: string;
  /** Denormalised so the feed still reads correctly after a deletion. */
  resource_name: string;
  status: WebhookDeliveryStatus;
  /** Attempts made so far (max 4). */
  attempt: number;
  /** The most recent attempt's HTTP status; null before the first attempt and on a transport error. */
  response_status?: WebhookDeliveryResponseStatus;
  /** How long the most recent attempt took; null before the first attempt. */
  duration_ms?: WebhookDeliveryDurationMs;
  /** When the next retry is due; null once terminal. */
  next_attempt_at?: WebhookDeliveryNextAttemptAt;
  /** The original delivery id when this row is a replay. */
  redelivery_of?: WebhookDeliveryRedeliveryOf;
  created_at: string;
  updated_at: string;
}
