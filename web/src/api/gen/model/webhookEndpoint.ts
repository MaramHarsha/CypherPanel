/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 * OpenAPI spec version: 0.3.0
 */
import type { WebhookEndpointEventsItem } from './webhookEndpointEventsItem.ts';
import type { WebhookEndpointHealth } from './webhookEndpointHealth.ts';

export interface WebhookEndpoint {
  /** whe_… prefixed ID. */
  id: string;
  project_id: string;
  /** The http(s) receiver. It is the endpoint's identity — there is no name. */
  url: string;
  events: WebhookEndpointEventsItem[];
  enabled: boolean;
  /** Derived from the ten most recent terminal deliveries, never stored. unknown = disabled or nothing delivered yet; healthy = all ten succeeded; degraded = the most recent succeeded but at least one failed; failing = the most recent exhausted its attempts. */
  health: WebhookEndpointHealth;
  /** @nullable */
  last_delivery_at?: string | null;
  created_at: string;
  updated_at: string;
}
