/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 *
 * Every response — success, error and SSE stream alike — carries an `X-Request-Id` header (`components/headers/RequestId`), and every JSON error body repeats it as `trace_id`. It is the value to quote in a bug report and the key to search for in `GET /api/v1/panel/logs`. Individual responses reference the header only where a generated client benefits; it is present on all of them.
 * OpenAPI spec version: 0.3.0
 */
import type { QuotaUsageDimension } from './quotaUsageDimension.ts';
import type { QuotaUsageState } from './quotaUsageState.ts';

export interface QuotaUsage {
  dimension: QuotaUsageDimension;
  used: number;
  /**
     * Null means uncapped, which reads as no cap rather than as zero.
     * @nullable
     */
  limit?: number | null;
  /** warn at 90%, exceeded at 100%. Uncapped is always ok. */
  state: QuotaUsageState;
  /**
     * TEAM REPORTS ONLY: the sum of this team's project caps on this dimension. Absent on a project report, which has nothing below it to sum — absent means "does not apply", never zero.
     *
     * It may exceed `limit`, and that is not an error. Over-commitment is allowed on purpose: thin provisioning is the normal case, eleven clients do not peak together, and forcing an operator to keep two numbers in lockstep would buy a refusal at the moment they were being careful. It is shown, not forbidden.
     */
  committed?: number;
  /** TEAM REPORTS ONLY: how many projects in this team have no cap of their own on this dimension. It travels with `committed` because the sum alone is the more flattering half of the truth — zero committed across eleven uncapped projects reads as "nothing promised" when it means "nothing bounded". */
  uncapped_projects?: number;
}
