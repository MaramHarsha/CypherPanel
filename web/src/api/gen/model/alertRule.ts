/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 *
 * Every response — success, error and SSE stream alike — carries an `X-Request-Id` header (`components/headers/RequestId`), and every JSON error body repeats it as `trace_id`. It is the value to quote in a bug report and the key to search for in `GET /api/v1/panel/logs`. Individual responses reference the header only where a generated client benefits; it is present on all of them.
 * OpenAPI spec version: 0.3.0
 */
import type { AlertRuleSignal } from './alertRuleSignal.ts';
import type { AlertRuleState } from './alertRuleState.ts';
import type { AlertRuleTargetKind } from './alertRuleTargetKind.ts';
import type { AlertRuleThresholdUnit } from './alertRuleThresholdUnit.ts';

export interface AlertRule {
  /** alr_… prefixed ID. */
  id: string;
  target_kind: AlertRuleTargetKind;
  target_id: string;
  target_name: string;
  signal: AlertRuleSignal;
  threshold: number;
  /** Stored, not derived: 90 is percent for a server's memory and bytes for an application's disk, and a rule must never be reinterpreted in a unit it was not written in. */
  threshold_unit: AlertRuleThresholdUnit;
  /** The `for`. One metrics bucket is the floor: sample-level data does not exist on the plane, which is what makes even the cheapest rule immune to a single spike. */
  window_seconds: number;
  notifier_id: string;
  notifier_name: string;
  enabled: boolean;
  /** All four are shown, including the two that deliver nothing: a rule quiet for a bad reason must not look like one quiet for a good reason. */
  state: AlertRuleState;
  state_since: string;
  /** The rule's name, rendered from the row. Everything that displays a rule shows this. */
  sentence: string;
}
