/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * CypherPanel API
 * Operator-facing control-plane API covering Phases 1–3: authentication and teams/roles, the server lifecycle, projects/environments, applications and the deploy pipeline, managed databases with S3 backups/restore, preview environments, notifications, and scheduled tasks. Authentication is a bearer session token from /api/v1/auth/login; the GitHub webhook authenticates by per-application HMAC instead. Every project-scoped route is authorized by team membership (a non-member sees 404, insufficient rank 403).
 *
 * Every response — success, error and SSE stream alike — carries an `X-Request-Id` header (`components/headers/RequestId`), and every JSON error body repeats it as `trace_id`. It is the value to quote in a bug report and the key to search for in `GET /api/v1/panel/logs`. Individual responses reference the header only where a generated client benefits; it is present on all of them.
 * OpenAPI spec version: 0.3.0
 */
import type { ServerRole } from './serverRole.ts';
import type { ServerStatus } from './serverStatus.ts';

export interface Server {
  /** srv_… prefixed ID; also the CN of the agent's certificate. */
  id: string;
  name: string;
  /** Single status vocabulary (ui-principles §5). */
  status: ServerStatus;
  /** Orchestrator driver the agent reports (e.g. docker). */
  driver: string;
  /** Agent-asserted role (builder-role-and-relay.md §1). "all" builds and runs; "builder" only builds; "worker" only runs. Read-only — set via the agent's --role flag, recorded from heartbeats. */
  role?: ServerRole;
  agent_version: string;
  hostname: string;
  /** Where DNS records for this server's applications point (dns-automation.md §3.4). Empty until an operator sets it; the plane cannot learn it, because the agent dials out and the heartbeat carries no address. */
  public_address?: string;
  /** The Docker data root's filesystem, as of the last heartbeat. `0` means not reported — an older agent, or a host where it could not be read — which a client should show as unknown, never as full. */
  disk_total_bytes?: number;
  disk_free_bytes?: number;
  /** Whether the server is past the panel's disk threshold (`CYPHERD_DISK_WARN_PERCENT`, default 85). Crossing it, and crossing back, writes one notification-inbox item for the panel's owners and admins — on the transition, never on every heartbeat. */
  disk_low?: boolean;
  /** Whether an agent has completed enrollment. */
  enrolled: boolean;
  /** @nullable */
  enrolled_at?: string | null;
  /**
     * Time of the most recent heartbeat; null if never seen.
     * @nullable
     */
  last_seen_at?: string | null;
  created_at: string;
}
