# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: vert
name: Vert
description: The next-generation file converter. Open source, fully local and free forever.
category: dev-tools
resources:
  applications:
    - name: vert
      image: ghcr.io/vert-sh/vert@sha256:6492f2a88e977e7ce82fd26f49028a17575fee8cac38604f585bc5773298cbc8
      port: 80
      route: true
      health:
        kind: tcp
      env:
        PUB_ENV: production
        PUB_HOSTNAME: https://{{domain}}
        PUB_PORT: "80"
        PUB_VERT_URL: https://{{domain}}
first_login:
  kind: none
  note: "There is no sign-in — open the domain and use it."
