# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: linkding-plus
name: Linkding Plus
description: A self-hosted bookmark manager designed to be minimal, fast, and easy to set up. (Includes feature for archiving websites as HTML snapshots)
category: productivity
version: 1.46.1-plus
resources:
  applications:
    - name: linkding-plus
      image: sissbruecker/linkding@sha256:165d07e383a8da30585b7af332cdcdc6700b3a6bd5fb2c3e90b0e11a27c9e54a
      port: 9090
      route: true
      health:
        kind: tcp
      volumes:
        - name: linkding-data
          path: /etc/linkding/data
      env:
        LD_SUPERUSER_NAME: '{{secret.16}}'
        LD_SUPERUSER_PASSWORD: '{{secret.16}}'
