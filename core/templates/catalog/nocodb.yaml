# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: nocodb
name: NocoDB
description: NocoDB is an open source Airtable alternative. Turns any MySQL, PostgreSQL, SQL Server, SQLite & MariaDB into a smart-spreadsheet.
category: productivity
version: 2026.08.0
resources:
  applications:
    - name: nocodb
      image: nocodb/nocodb@sha256:f8050f95d2acef8a554c791b479147625d736ea1a8f8cf82380be4033b661b59
      port: 8080
      route: true
      health:
        kind: tcp
      volumes:
        - name: nocodb-data
          path: /usr/app/data/
first_login:
  kind: setup
  note: "Open the domain and sign up — the first account becomes the super admin."
