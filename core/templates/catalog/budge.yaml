# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: budge
name: Budge
description: A budgeting personal finance app.
category: finance
resources:
  applications:
    - name: budge
      image: lscr.io/linuxserver/budge@sha256:187a8087588bd1d8dc6601a81a771549fcfe3cb72388633acb5e19dea11f338e
      port: 80
      route: true
      health:
        kind: tcp
      volumes:
        - name: budge-config
          path: /config
      env:
        PGID: "1000"
        PUID: "1000"
        TZ: Europe/Madrid
