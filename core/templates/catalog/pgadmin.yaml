# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: pgadmin
name: pgAdmin
description: pgAdmin is a web-based database management tool for administering your PostgreSQL databases through a user-friendly interface.
category: storage
resources:
  applications:
    - name: pgadmin
      image: dpage/pgadmin4@sha256:2f4ce946ddf8360680d7eff4eaba1d91859eb6b4003e6623bad5c63a322c2f4d
      port: 80
      route: true
      health:
        kind: tcp
      volumes:
        - name: pgadmin-data
          path: /var/lib/pgadmin
      env:
        PGADMIN_DEFAULT_EMAIL: ""
        PGADMIN_DEFAULT_PASSWORD: ""
first_login:
  kind: setup
  note: "Set PGADMIN_DEFAULT_EMAIL and PGADMIN_DEFAULT_PASSWORD in the app's environment variables and redeploy — pgAdmin will not start until both are set."
