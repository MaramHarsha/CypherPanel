# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: chroma
name: Chroma
description: Chroma is the open-source search and retrieval database for AI applications.
category: ai
version: 1.0.15
resources:
  applications:
    - name: chromadb
      image: chromadb/chroma@sha256:663aa18087cd698aacc3c16c1ea0c1585be9a6392e2fd80c3086c371e2f89363
      port: 8000
      route: true
      health:
        kind: tcp
      volumes:
        - name: chroma-data
          path: /data
      env:
        IS_PERSISTENT: "TRUE"
        PERSIST_DIRECTORY: /data
