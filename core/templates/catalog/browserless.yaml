# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: browserless
name: Browserless
description: A headless Chrome browser as a service.
category: dev-tools
resources:
  applications:
    - name: browserless
      image: ghcr.io/browserless/chromium@sha256:13d15859796b8a0f72cf713e86c7dd02f181d23f2efa4a377ba683b3e9a7bbf6
      port: 3000
      route: true
      health:
        kind: tcp
      env:
        TOKEN: '{{secret.16}}'
first_login:
  kind: none
  note: "There is no sign-in — open the domain and use it."
