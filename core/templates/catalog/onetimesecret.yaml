# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: onetimesecret
name: Onetimesecret
description: Share sensitive information securely with self-destructing links that are only viewable once.
category: security
version: 0.26.4
resources:
  databases:
    - name: cache
      engine: redis
      version: "8"
  applications:
    - name: onetimesecret
      image: onetimesecret/onetimesecret@sha256:a1773ee0f9b826e1c417ede082535b6e3b5e0a0da79206272c68ee68d763ad0e
      port: 3000
      route: true
      health:
        kind: tcp
      env:
        AUTH_AUTOVERIFY: "true"
        AUTH_SIGNUP: "true"
        COLONEL: admin@example.com
        HOST: localhost
        RACK_ENV: production
        REDIS_URL: redis://:{{db.cache.password}}@{{db.cache.host}}:6379/0
        SECRET: '{{secret.16}}'
        SSL: "false"
