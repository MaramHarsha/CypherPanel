# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: rivet-engine
name: Rivet Engine
description: Build and scale stateful workloads with long-lived processes
category: dev-tools
version: 2.2.0
resources:
  applications:
    - name: rivet-engine
      image: rivetdev/engine@sha256:ac5ac95af42ff1a53a41a0750be0fbd8bcbae0a18aa42f55aac72fd0b66ad8d6
      port: 6420
      route: true
      health:
        kind: tcp
      volumes:
        - name: rivet-data
          path: /data
      env:
        RIVET__AUTH__ADMIN_TOKEN: '{{secret.16}}'
        RIVET__FILE_SYSTEM__PATH: /data
