# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: appsmith
name: Appsmith
description: A low-code application platform for building internal tools.
category: productivity
resources:
  applications:
    - name: appsmith
      image: appsmith/appsmith-ce@sha256:6e6ff29dc25f359793a6031f1ec7acf6bbdbb95e36f3a33e366ba4c14a929b3a
      port: 80
      route: true
      health:
        kind: tcp
      volumes:
        - name: stacks-data
          path: /appsmith-stacks
      env:
        APPSMITH_DISABLE_INTERCOM: "true"
        APPSMITH_DISABLE_TELEMETRY: "false"
        APPSMITH_MAIL_ENABLED: "false"
        APPSMITH_SENTRY_DSN: ""
        APPSMITH_SMART_LOOK_ID: ""
first_login:
  kind: setup
  note: "Open the domain and sign up — the first account becomes the instance admin."
