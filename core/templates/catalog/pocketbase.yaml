# Converted from Coolify's compose template library by core/cmd/coolify-import.
# Reviewed and committed like a hand-written template; the catalog test is the gate.
schema: v1
slug: pocketbase
name: Pocketbase
description: Open Source backend for your next SaaS and Mobile app in 1 file
category: dev-tools
resources:
  applications:
    - name: pocketbase
      image: ghcr.io/coollabsio/pocketbase@sha256:d97b76634af15bd84fef1f0350e8eea7c1bd17747c6b33dd033a437321940e63
      port: 8080
      route: true
      health:
        kind: tcp
      volumes:
        - name: pocketbase-data
          path: /app/pb_data
        - name: pocketbase-hooks
          path: /app/pb_hooks
