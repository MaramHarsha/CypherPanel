// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: volume_backups.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createVolumeBackupRecord = `-- name: CreateVolumeBackupRecord :one
INSERT INTO volume_backup_records (id, volume_backup_id, volume_name)
VALUES ($1, $2, $3)
RETURNING id, volume_backup_id, volume_name, object_key, size_bytes, status, detail, started_at, finished_at, created_at
`

type CreateVolumeBackupRecordParams struct {
	ID             string
	VolumeBackupID string
	VolumeName     string
}

func (q *Queries) CreateVolumeBackupRecord(ctx context.Context, arg CreateVolumeBackupRecordParams) (VolumeBackupRecord, error) {
	row := q.db.QueryRow(ctx, createVolumeBackupRecord, arg.ID, arg.VolumeBackupID, arg.VolumeName)
	var i VolumeBackupRecord
	err := row.Scan(
		&i.ID,
		&i.VolumeBackupID,
		&i.VolumeName,
		&i.ObjectKey,
		&i.SizeBytes,
		&i.Status,
		&i.Detail,
		&i.StartedAt,
		&i.FinishedAt,
		&i.CreatedAt,
	)
	return i, err
}

const deleteVolumeBackup = `-- name: DeleteVolumeBackup :exec
DELETE FROM volume_backups WHERE application_id = $1
`

func (q *Queries) DeleteVolumeBackup(ctx context.Context, applicationID string) error {
	_, err := q.db.Exec(ctx, deleteVolumeBackup, applicationID)
	return err
}

const deleteVolumeBackupRecords = `-- name: DeleteVolumeBackupRecords :exec
DELETE FROM volume_backup_records WHERE id = ANY($1::text[])
`

func (q *Queries) DeleteVolumeBackupRecords(ctx context.Context, dollar_1 []string) error {
	_, err := q.db.Exec(ctx, deleteVolumeBackupRecords, dollar_1)
	return err
}

const getVolumeBackup = `-- name: GetVolumeBackup :one
SELECT id, application_id, target_id, schedule, retention_count, enabled, last_run_at, last_status, created_at, updated_at FROM volume_backups WHERE id = $1
`

func (q *Queries) GetVolumeBackup(ctx context.Context, id string) (VolumeBackup, error) {
	row := q.db.QueryRow(ctx, getVolumeBackup, id)
	var i VolumeBackup
	err := row.Scan(
		&i.ID,
		&i.ApplicationID,
		&i.TargetID,
		&i.Schedule,
		&i.RetentionCount,
		&i.Enabled,
		&i.LastRunAt,
		&i.LastStatus,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getVolumeBackupByApplication = `-- name: GetVolumeBackupByApplication :one
SELECT id, application_id, target_id, schedule, retention_count, enabled, last_run_at, last_status, created_at, updated_at FROM volume_backups WHERE application_id = $1
`

func (q *Queries) GetVolumeBackupByApplication(ctx context.Context, applicationID string) (VolumeBackup, error) {
	row := q.db.QueryRow(ctx, getVolumeBackupByApplication, applicationID)
	var i VolumeBackup
	err := row.Scan(
		&i.ID,
		&i.ApplicationID,
		&i.TargetID,
		&i.Schedule,
		&i.RetentionCount,
		&i.Enabled,
		&i.LastRunAt,
		&i.LastStatus,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getVolumeBackupRecord = `-- name: GetVolumeBackupRecord :one
SELECT id, volume_backup_id, volume_name, object_key, size_bytes, status, detail, started_at, finished_at, created_at FROM volume_backup_records WHERE id = $1
`

func (q *Queries) GetVolumeBackupRecord(ctx context.Context, id string) (VolumeBackupRecord, error) {
	row := q.db.QueryRow(ctx, getVolumeBackupRecord, id)
	var i VolumeBackupRecord
	err := row.Scan(
		&i.ID,
		&i.VolumeBackupID,
		&i.VolumeName,
		&i.ObjectKey,
		&i.SizeBytes,
		&i.Status,
		&i.Detail,
		&i.StartedAt,
		&i.FinishedAt,
		&i.CreatedAt,
	)
	return i, err
}

const listEnabledVolumeBackupSchedules = `-- name: ListEnabledVolumeBackupSchedules :many
SELECT id, application_id, target_id, schedule, retention_count, enabled, last_run_at, last_status, created_at, updated_at FROM volume_backups WHERE enabled = true AND schedule <> ''
`

func (q *Queries) ListEnabledVolumeBackupSchedules(ctx context.Context) ([]VolumeBackup, error) {
	rows, err := q.db.Query(ctx, listEnabledVolumeBackupSchedules)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []VolumeBackup{}
	for rows.Next() {
		var i VolumeBackup
		if err := rows.Scan(
			&i.ID,
			&i.ApplicationID,
			&i.TargetID,
			&i.Schedule,
			&i.RetentionCount,
			&i.Enabled,
			&i.LastRunAt,
			&i.LastStatus,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listVolumeBackupRecords = `-- name: ListVolumeBackupRecords :many
SELECT id, volume_backup_id, volume_name, object_key, size_bytes, status, detail, started_at, finished_at, created_at FROM volume_backup_records
WHERE volume_backup_id = $1
ORDER BY created_at DESC
LIMIT $2
`

type ListVolumeBackupRecordsParams struct {
	VolumeBackupID string
	Limit          int32
}

func (q *Queries) ListVolumeBackupRecords(ctx context.Context, arg ListVolumeBackupRecordsParams) ([]VolumeBackupRecord, error) {
	rows, err := q.db.Query(ctx, listVolumeBackupRecords, arg.VolumeBackupID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []VolumeBackupRecord{}
	for rows.Next() {
		var i VolumeBackupRecord
		if err := rows.Scan(
			&i.ID,
			&i.VolumeBackupID,
			&i.VolumeName,
			&i.ObjectKey,
			&i.SizeBytes,
			&i.Status,
			&i.Detail,
			&i.StartedAt,
			&i.FinishedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listVolumeRecordsBeyondRetention = `-- name: ListVolumeRecordsBeyondRetention :many
SELECT id, volume_backup_id, volume_name, object_key, size_bytes, status, detail, started_at, finished_at, created_at FROM volume_backup_records
WHERE volume_backup_id = $1 AND volume_name = $2 AND status = 'succeeded'
ORDER BY created_at DESC
OFFSET $3
`

type ListVolumeRecordsBeyondRetentionParams struct {
	VolumeBackupID string
	VolumeName     string
	Offset         int32
}

// Retention is per VOLUME, not per schedule: keeping "the last 7" across a
// two-volume application would keep three of one and four of the other.
func (q *Queries) ListVolumeRecordsBeyondRetention(ctx context.Context, arg ListVolumeRecordsBeyondRetentionParams) ([]VolumeBackupRecord, error) {
	rows, err := q.db.Query(ctx, listVolumeRecordsBeyondRetention, arg.VolumeBackupID, arg.VolumeName, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []VolumeBackupRecord{}
	for rows.Next() {
		var i VolumeBackupRecord
		if err := rows.Scan(
			&i.ID,
			&i.VolumeBackupID,
			&i.VolumeName,
			&i.ObjectKey,
			&i.SizeBytes,
			&i.Status,
			&i.Detail,
			&i.StartedAt,
			&i.FinishedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setVolumeBackupLastRun = `-- name: SetVolumeBackupLastRun :exec
UPDATE volume_backups SET last_run_at = $2, last_status = $3, updated_at = now() WHERE id = $1
`

type SetVolumeBackupLastRunParams struct {
	ID         string
	LastRunAt  pgtype.Timestamptz
	LastStatus string
}

func (q *Queries) SetVolumeBackupLastRun(ctx context.Context, arg SetVolumeBackupLastRunParams) error {
	_, err := q.db.Exec(ctx, setVolumeBackupLastRun, arg.ID, arg.LastRunAt, arg.LastStatus)
	return err
}

const updateVolumeBackupRecord = `-- name: UpdateVolumeBackupRecord :exec
UPDATE volume_backup_records
SET object_key = $2, size_bytes = $3, status = $4, detail = $5, finished_at = now()
WHERE id = $1
`

type UpdateVolumeBackupRecordParams struct {
	ID        string
	ObjectKey string
	SizeBytes int64
	Status    string
	Detail    string
}

func (q *Queries) UpdateVolumeBackupRecord(ctx context.Context, arg UpdateVolumeBackupRecordParams) error {
	_, err := q.db.Exec(ctx, updateVolumeBackupRecord,
		arg.ID,
		arg.ObjectKey,
		arg.SizeBytes,
		arg.Status,
		arg.Detail,
	)
	return err
}

const upsertVolumeBackup = `-- name: UpsertVolumeBackup :one
INSERT INTO volume_backups (id, application_id, target_id, schedule, retention_count, enabled)
VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT (application_id) DO UPDATE
SET target_id = EXCLUDED.target_id,
    schedule = EXCLUDED.schedule,
    retention_count = EXCLUDED.retention_count,
    enabled = EXCLUDED.enabled,
    updated_at = now()
RETURNING id, application_id, target_id, schedule, retention_count, enabled, last_run_at, last_status, created_at, updated_at
`

type UpsertVolumeBackupParams struct {
	ID             string
	ApplicationID  string
	TargetID       string
	Schedule       string
	RetentionCount int32
	Enabled        bool
}

func (q *Queries) UpsertVolumeBackup(ctx context.Context, arg UpsertVolumeBackupParams) (VolumeBackup, error) {
	row := q.db.QueryRow(ctx, upsertVolumeBackup,
		arg.ID,
		arg.ApplicationID,
		arg.TargetID,
		arg.Schedule,
		arg.RetentionCount,
		arg.Enabled,
	)
	var i VolumeBackup
	err := row.Scan(
		&i.ID,
		&i.ApplicationID,
		&i.TargetID,
		&i.Schedule,
		&i.RetentionCount,
		&i.Enabled,
		&i.LastRunAt,
		&i.LastStatus,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
