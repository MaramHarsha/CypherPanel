// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: revisions.sql

package db

import (
	"context"
)

const createRevision = `-- name: CreateRevision :one
INSERT INTO revisions (id, application_id, source_commit, config_snapshot)
VALUES ($1, $2, $3, $4)
RETURNING id, application_id, image, source_commit, config_snapshot, created_at
`

type CreateRevisionParams struct {
	ID             string
	ApplicationID  string
	SourceCommit   string
	ConfigSnapshot []byte
}

func (q *Queries) CreateRevision(ctx context.Context, arg CreateRevisionParams) (Revision, error) {
	row := q.db.QueryRow(ctx, createRevision,
		arg.ID,
		arg.ApplicationID,
		arg.SourceCommit,
		arg.ConfigSnapshot,
	)
	var i Revision
	err := row.Scan(
		&i.ID,
		&i.ApplicationID,
		&i.Image,
		&i.SourceCommit,
		&i.ConfigSnapshot,
		&i.CreatedAt,
	)
	return i, err
}

const getRevision = `-- name: GetRevision :one
SELECT id, application_id, image, source_commit, config_snapshot, created_at FROM revisions WHERE id = $1
`

func (q *Queries) GetRevision(ctx context.Context, id string) (Revision, error) {
	row := q.db.QueryRow(ctx, getRevision, id)
	var i Revision
	err := row.Scan(
		&i.ID,
		&i.ApplicationID,
		&i.Image,
		&i.SourceCommit,
		&i.ConfigSnapshot,
		&i.CreatedAt,
	)
	return i, err
}

const listRevisionsByApplication = `-- name: ListRevisionsByApplication :many
SELECT id, application_id, image, source_commit, config_snapshot, created_at FROM revisions WHERE application_id = $1 ORDER BY created_at DESC
`

func (q *Queries) ListRevisionsByApplication(ctx context.Context, applicationID string) ([]Revision, error) {
	rows, err := q.db.Query(ctx, listRevisionsByApplication, applicationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Revision{}
	for rows.Next() {
		var i Revision
		if err := rows.Scan(
			&i.ID,
			&i.ApplicationID,
			&i.Image,
			&i.SourceCommit,
			&i.ConfigSnapshot,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setRevisionImage = `-- name: SetRevisionImage :one
UPDATE revisions SET image = $2 WHERE id = $1 RETURNING id, application_id, image, source_commit, config_snapshot, created_at
`

type SetRevisionImageParams struct {
	ID    string
	Image string
}

func (q *Queries) SetRevisionImage(ctx context.Context, arg SetRevisionImageParams) (Revision, error) {
	row := q.db.QueryRow(ctx, setRevisionImage, arg.ID, arg.Image)
	var i Revision
	err := row.Scan(
		&i.ID,
		&i.ApplicationID,
		&i.Image,
		&i.SourceCommit,
		&i.ConfigSnapshot,
		&i.CreatedAt,
	)
	return i, err
}
