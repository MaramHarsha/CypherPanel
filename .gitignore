# Binaries — build output, never committed (see Makefile targets)
/bin/
/dist/
*.exe
/agent
/core
/cypherctl

# Go
/vendor/
coverage.out

# Node / Next.js (Phase 2)
node_modules/
.next/

# Local environment & secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
*.crt

# Reference clones of other open-source hosting panels (research only, not part of this repo)
/Website-references/

# Editor / OS noise
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store
