# Coolify template import report

Generated by `core/cmd/coolify-import` over 365 source templates: **150 converted**, 215 refused.

## Why templates were refused

| Blocker | Templates |
|---|---|
| Overrides the image command or entrypoint | 65 |
| Orders one application after another | 58 |
| Declares no port, and its image exposes none | 55 |
| Applications address each other by hostname | 44 |
| Reuses one generated value in several places | 43 |
| Mounts a host path | 34 |
| Never tells the application its database password | 28 |
| Not listed by Coolify either | 28 |
| Publishes fixed host ports | 24 |
| Needs privileged access, capabilities, or devices | 20 |
| Publishes more than one public URL | 18 |
| Includes a one-shot job container | 15 |
| Needs a generated value shape the schema has no token for | 13 |
| Needs a fixed identity or hostname | 12 |
| Pins a CPU architecture | 12 |
| Serves nothing over HTTP | 12 |
| Needs custom networking | 10 |
| Uses a compose feature the schema has no field for | 8 |
| Image could not be resolved to an immutable reference | 5 |
| Interpolates a variable with no default into a larger value | 5 |
| Routes a sub-path rather than a domain | 5 |
| Superseded by a curated entry | 4 |
| Produces a template the schema rejects | 2 |
| Addresses a resource the install would not create | 1 |

## Refused templates

### activepieces

- no application is given the cache database's password, which managed databases always require

### affine

- service "affine_migration" overrides the image command; templates configure through environment only
- service "affine_migration" is a one-shot job (restart: on-failure); templates create long-running applications
- service "affine" waits on "postgres"; templates order databases before applications and nothing else
- service "affine_migration" waits on "postgres"; templates order databases before applications and nothing else
- DATABASE_URL dials the sibling service "postgres"; application containers are named per revision and have no stable address
- no application is given the cache database's password, which managed databases always require
- generated value SERVICE_PASSWORD_POSTGRES is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_POSTGRES is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### alexandrie

- service "frontend" waits on "backend"; templates order databases before applications and nothing else
- service "backend" waits on "rustfs"; templates order databases before applications and nothing else
- services "frontend" and "backend" both publish a public URL; a template routes at most one
- services "frontend" and "rustfs" both publish a public URL; a template routes at most one
- service "rustfs" names no port and its image exposes none unambiguously; the schema requires one per application
- MINIO_ENDPOINT dials the sibling service "rustfs"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_RUSTFS is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_RUSTFS is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### anythingllm

- service "anything-llm" adds Linux capabilities
- service "anything-llm" runs as a fixed uid

### appflowy

- service "minio" overrides the image command; templates configure through environment only
- service "nginx" waits on "appflowy_cloud"; templates order databases before applications and nothing else
- service "nginx" waits on "gotrue"; templates order databases before applications and nothing else
- service "nginx" waits on "admin_frontend"; templates order databases before applications and nothing else
- service "appflowy_web" waits on "appflowy_cloud"; templates order databases before applications and nothing else
- service "appflowy_cloud" waits on "gotrue"; templates order databases before applications and nothing else
- service "appflowy_cloud" waits on "postgres"; templates order databases before applications and nothing else
- service "gotrue" waits on "postgres"; templates order databases before applications and nothing else
- service "admin_frontend" waits on "gotrue"; templates order databases before applications and nothing else
- service "admin_frontend" waits on "appflowy_cloud"; templates order databases before applications and nothing else
- service "ai" waits on "postgres"; templates order databases before applications and nothing else
- service "appflowy_worker" waits on "postgres"; templates order databases before applications and nothing else
- services "nginx" and "appflowy_web" both publish a public URL; a template routes at most one
- service "gotrue" names no port and its image exposes none unambiguously; the schema requires one per application
- service "appflowy_worker" names no port and its image exposes none unambiguously; the schema requires one per application
- APPFLOWY_GOTRUE_BASE_URL dials the sibling service "gotrue"; application containers are named per revision and have no stable address
- AI_SERVER_HOST dials the sibling service "ai"; application containers are named per revision and have no stable address
- APPFLOWY_DATABASE_URL dials the sibling service "postgres"; application containers are named per revision and have no stable address
- APPFLOWY_S3_MINIO_URL dials the sibling service "minio"; application containers are named per revision and have no stable address
- DATABASE_URL dials the sibling service "postgres"; application containers are named per revision and have no stable address
- GOTRUE_DB_DRIVER dials the sibling service "postgres"; application containers are named per revision and have no stable address
- AI_DATABASE_URL dials the sibling service "postgres"; application containers are named per revision and have no stable address
- AI_MINIO_URL dials the sibling service "minio"; application containers are named per revision and have no stable address
- APPFLOWY_WORKER_DATABASE_URL dials the sibling service "postgres"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_JWT is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_MINIO is used 4 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_POSTGRES is used 5 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MINIO is used 4 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_POSTGRES is used 5 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### apprise-api

- service "apprise-api" mounts "/apprise-api/config:/config" from the host; templates declare named volumes only

### appwrite

- declares custom networks; every resource joins its environment network
- service "appwrite-realtime" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-audits" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-webhooks" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-deletes" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-databases" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-builds" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-certificates" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-functions" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-mails" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-messaging" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-migrations" overrides the image entrypoint; templates configure through environment only
- service "appwrite-task-maintenance" overrides the image entrypoint; templates configure through environment only
- service "appwrite-task-stats-resources" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-stats-resources" overrides the image entrypoint; templates configure through environment only
- service "appwrite-worker-stats-usage" overrides the image entrypoint; templates configure through environment only
- service "appwrite-task-scheduler-functions" overrides the image entrypoint; templates configure through environment only
- service "appwrite-task-scheduler-executions" overrides the image entrypoint; templates configure through environment only
- service "appwrite-task-scheduler-messages" overrides the image entrypoint; templates configure through environment only
- service "appwrite-browser" needs a fixed hostname
- service "openruntimes-executor" needs a fixed hostname
- service "openruntimes-executor" joins a custom network
- service "openruntimes-executor" overrides the stop signal
- service "appwrite-worker-functions" waits on "openruntimes-executor"; templates order databases before applications and nothing else
- services "appwrite" and "appwrite-console" both publish a public URL; a template routes at most one
- services "appwrite" and "appwrite-realtime" both publish a public URL; a template routes at most one
- service "appwrite-console" routes on the sub-path "/console"; templates route a whole domain
- service "appwrite-realtime" routes on the sub-path "/v1/realtime"; templates route a whole domain
- service "appwrite-browser" names no port and its image exposes none unambiguously; the schema requires one per application
- service "openruntimes-executor" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only
- service "openruntimes-executor" mounts "/tmp:/tmp:rw" from the host; templates declare named volumes only
- OPR_EXECUTOR_RUNTIMES interpolates a variable with no default into "${_APP_FUNCTIONS_RUNTIMES},${_APP_SITES_RUNTIMES}"; the installed value would be malformed
- no application is given the cache database's password, which managed databases always require
- generated value SERVICE_PASSWORD_64_APPWRITE is used 24 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### argilla

- service "worker" overrides the image command; templates configure through environment only
- service "elasticsearch" sets resource ulimits
- service "argilla" waits on "elasticsearch"; templates order databases before applications and nothing else
- service "worker" waits on "elasticsearch"; templates order databases before applications and nothing else
- service "elasticsearch" names no port and its image exposes none unambiguously; the schema requires one per application
- ARGILLA_ELASTICSEARCH dials the sibling service "elasticsearch"; application containers are named per revision and have no stable address
- ARGILLA_HOME_PATH dials the sibling service "argilla"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_APIKEY is used 4 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_ARGILLA is used 4 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_AUTHSECRET is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### authentik

- service "authentik-server" overrides the image command; templates configure through environment only
- service "authentik-worker" overrides the image command; templates configure through environment only
- service "authentik-worker" runs as a fixed uid
- service "authentik-server" mounts "./media:/media" from the host; templates declare named volumes only
- service "authentik-server" mounts "./custom-templates:/templates" from the host; templates declare named volumes only
- service "authentik-worker" names no port and its image exposes none unambiguously; the schema requires one per application
- service "authentik-worker" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only
- service "authentik-worker" mounts "./media:/media" from the host; templates declare named volumes only
- service "authentik-worker" mounts "./certs:/certs" from the host; templates declare named volumes only
- service "authentik-worker" mounts "./custom-templates:/templates" from the host; templates declare named volumes only
- generated value SERVICE_PASSWORD_64_AUTHENTIKSERVER is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### autobase

- service "autobase" pins a CPU architecture
- service "autobase-db" pins a CPU architecture
- service "autobase-api" pins a CPU architecture
- service "autobase" waits on "autobase-api"; templates order databases before applications and nothing else
- service "autobase-api" waits on "autobase-db"; templates order databases before applications and nothing else
- service "autobase-api" names no port and its image exposes none unambiguously; the schema requires one per application
- service "autobase-api" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only
- service "autobase-api" mounts "/tmp/ansible:/tmp/ansible" from the host; templates declare named volumes only
- PG_CONSOLE_API_HOST dials the sibling service "autobase-api"; application containers are named per revision and have no stable address
- PG_CONSOLE_DB_HOST dials the sibling service "autobase-db"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_POSTGRES is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_UI is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### azimutt

- marked `# ignore: true` upstream — Coolify itself does not list it

### beszel-agent

- service "beszel-agent" sets a custom network mode
- no service declares a public port; there is nothing to route the install-time domain at
- service "beszel-agent" names no port and its image exposes none unambiguously; the schema requires one per application
- service "beszel-agent" mounts "/var/run/docker.sock:/var/run/docker.sock:ro" from the host; templates declare named volumes only

### beszel

- service "beszel-agent" sets a custom network mode
- service "beszel-agent" names no port and its image exposes none unambiguously; the schema requires one per application
- service "beszel-agent" mounts "/var/run/docker.sock:/var/run/docker.sock:ro" from the host; templates declare named volumes only

### bitcoin-core

- service "bitcoin-core" overrides the image command; templates configure through environment only
- no service declares a public port; there is nothing to route the install-time domain at
- service "bitcoin-core" names no port and its image exposes none unambiguously; the schema requires one per application

### bluesky-pds

- service "pds" overrides the image command; templates configure through environment only

### booklore

- marked `# ignore: true` upstream — Coolify itself does not list it

### bookstack

- service "bookstack" waits on "mariadb"; templates order databases before applications and nothing else
- DB_HOST dials the sibling service "mariadb"; application containers are named per revision and have no stable address
- MYSQL_DATABASE dials the sibling service "bookstack"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_MYSQL is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MYSQL is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### budibase

- service "minio-service" overrides the image command; templates configure through environment only
- service "watchtower-service" overrides the image command; templates configure through environment only
- service "app-service" waits on "worker-service"; templates order databases before applications and nothing else
- service "worker-service" waits on "minio-service"; templates order databases before applications and nothing else
- service "proxy-service" waits on "minio-service"; templates order databases before applications and nothing else
- service "proxy-service" waits on "worker-service"; templates order databases before applications and nothing else
- service "proxy-service" waits on "app-service"; templates order databases before applications and nothing else
- service "proxy-service" waits on "couchdb-service"; templates order databases before applications and nothing else
- service "minio-service" names no port and its image exposes none unambiguously; the schema requires one per application
- service "couchdb-service" names no port and its image exposes none unambiguously; the schema requires one per application
- service "watchtower-service" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only
- COUCH_DB_URL dials the sibling service "couchdb-service"; application containers are named per revision and have no stable address
- MINIO_URL dials the sibling service "minio-service"; application containers are named per revision and have no stable address
- WORKER_URL dials the sibling service "worker-service"; application containers are named per revision and have no stable address
- APPS_URL dials the sibling service "app-service"; application containers are named per revision and have no stable address
- APPS_UPSTREAM_URL dials the sibling service "app-service"; application containers are named per revision and have no stable address
- COUCHDB_UPSTREAM_URL dials the sibling service "couchdb-service"; application containers are named per revision and have no stable address
- MINIO_UPSTREAM_URL dials the sibling service "minio-service"; application containers are named per revision and have no stable address
- WATCHTOWER_UPSTREAM_URL dials the sibling service "watchtower-service"; application containers are named per revision and have no stable address
- WORKER_UPSTREAM_URL dials the sibling service "worker-service"; application containers are named per revision and have no stable address
- generated value SERVICE_BASE64_128_BUDIBASE is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_BASE64_64_BUDIBASEAPI is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_BASE64_64_BUDIBASEJWT is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_COUCHDB is used 4 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_MINIO is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_COUCHDB is used 4 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MINIO is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### buzz

- service "minio" overrides the image command; templates configure through environment only
- service "minio-init" overrides the image entrypoint; templates configure through environment only
- service "minio-init" is a one-shot job (restart: no); templates create long-running applications
- service "buzz" waits on "minio"; templates order databases before applications and nothing else
- service "buzz" waits on "minio-init"; templates order databases before applications and nothing else
- service "minio-init" waits on "minio"; templates order databases before applications and nothing else
- service "minio-init" names no port and its image exposes none unambiguously; the schema requires one per application
- BUZZ_S3_ENDPOINT dials the sibling service "minio"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_MINIO is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MINIO is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### calcom

- marked `# ignore: true` upstream — Coolify itself does not list it

### calibre-web-automated-book-downloader

- services "calibre-web-automated" and "calibre-web-downloader" both publish a public URL; a template routes at most one

### castopod

- service "castopod" reads SERVICE_REALBASE64_64_SALT, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it

### changedetection

- service "browser-sockpuppet-chrome" adds Linux capabilities
- service "browser-sockpuppet-chrome" needs a fixed hostname
- service "changedetection" waits on "browser-sockpuppet-chrome"; templates order databases before applications and nothing else
- service "browser-sockpuppet-chrome" names no port and its image exposes none unambiguously; the schema requires one per application
- PLAYWRIGHT_DRIVER_URL dials the sibling service "browser-sockpuppet-chrome"; application containers are named per revision and have no stable address

### chaskiq

- service "chaskiq" overrides the image entrypoint; templates configure through environment only
- service "sidekiq" overrides the image command; templates configure through environment only
- service "sidekiq" waits on "chaskiq"; templates order databases before applications and nothing else
- service "sidekiq" names no port and its image exposes none unambiguously; the schema requires one per application
- generated value SERVICE_PASSWORD_64_SECRET is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_ADMIN is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### chatwoot

- service "chatwoot" overrides the image command; templates configure through environment only
- service "chatwoot" overrides the image entrypoint; templates configure through environment only
- service "sidekiq" overrides the image command; templates configure through environment only
- service "chatwoot" waits on "postgres"; templates order databases before applications and nothing else
- service "sidekiq" waits on "postgres"; templates order databases before applications and nothing else
- POSTGRES_HOST dials the sibling service "postgres"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_CHATWOOT is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_POSTGRES is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_POSTGRES is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### checkmate

- service "mongodb" overrides the image command; templates configure through environment only
- service "checkmate" waits on "mongodb"; templates order databases before applications and nothing else
- DB_CONNECTION_STRING dials the sibling service "mongodb"; application containers are named per revision and have no stable address

### chibisafe

- BASE_API_URL dials the sibling service "chibisafe-server"; application containers are named per revision and have no stable address

### cloudflare-ddns

- service "cloudflare-ddns" drops Linux capabilities
- service "cloudflare-ddns" sets a custom network mode
- service "cloudflare-ddns" needs a read-only root filesystem
- service "cloudflare-ddns" sets container security options
- service "cloudflare-ddns" runs as a fixed uid
- no service declares a public port; there is nothing to route the install-time domain at
- service "cloudflare-ddns" names no port and its image exposes none unambiguously; the schema requires one per application

### cloudflared

- service "cloudflared" overrides the image command; templates configure through environment only
- service "cloudflared" sets a custom network mode
- no service declares a public port; there is nothing to route the install-time domain at
- service "cloudflared" names no port and its image exposes none unambiguously; the schema requires one per application

### cloudreve

- templates: invalid template: application "cloudreve": env key "CR_CONF_Database.Type" invalid

### coder

- marked `# ignore: true` upstream — Coolify itself does not list it

### codimd

- image nabo.codimd.dev/hackmdio/hackmd:latest could not be pinned to an immutable reference: registry answered 401 Unauthorized

### convex

- service "dashboard" waits on "backend"; templates order databases before applications and nothing else
- services "backend" and "dashboard" both publish a public URL; a template routes at most one

### cryptgeon

- no application is given the cache database's password, which managed databases always require

### cyberchef

- service "cyberchef" pins a CPU architecture

### dashy

- service "dashy" mounts "./dashy/conf.yml:/app/public/conf.yml" from the host; templates declare named volumes only

### denoKV

- service "denokv" overrides the image command; templates configure through environment only
- service "denokv" mounts "${COOLIFY_VOLUME_APP}:/data" from the host; templates declare named volumes only

### dify

- marked `# ignore: true` upstream — Coolify itself does not list it

### directus-with-postgresql

- service "postgresql" pins a CPU architecture
- service "directus" waits on "postgresql"; templates order databases before applications and nothing else
- DB_HOST dials the sibling service "postgresql"; application containers are named per revision and have no stable address
- no application is given the cache database's password, which managed databases always require
- generated value SERVICE_PASSWORD_POSTGRESQL is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_POSTGRESQL is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### diun

- service "diun" overrides the image command; templates configure through environment only
- no service declares a public port; there is nothing to route the install-time domain at
- service "diun" names no port and its image exposes none unambiguously; the schema requires one per application
- service "diun" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only

### docker-registry

- service "registry" overrides the image command; templates configure through environment only

### documenso

- service "documenso" overrides the image entrypoint; templates configure through environment only
- generated value SERVICE_PASSWORD_DOCUMENSO is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### dozzle-with-auth

- service "dozzle" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only

### dozzle

- service "dozzle" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only

### edgedb

- marked `# ignore: true` upstream — Coolify itself does not list it

### elasticsearch-with-kibana

- service "kibana-token-generator" overrides the image entrypoint; templates configure through environment only
- service "kibana-token-generator" is a one-shot job (restart: no); templates create long-running applications
- service "kibana" waits on "elasticsearch"; templates order databases before applications and nothing else
- service "kibana-token-generator" waits on "elasticsearch"; templates order databases before applications and nothing else
- service "elasticsearch" names no port and its image exposes none unambiguously; the schema requires one per application
- service "elasticsearch" mounts "/etc/localtime:/etc/localtime:ro" from the host; templates declare named volumes only
- service "kibana" mounts "/etc/localtime:/etc/localtime:ro" from the host; templates declare named volumes only
- service "kibana-token-generator" names no port and its image exposes none unambiguously; the schema requires one per application
- ELASTICSEARCH_HOSTS dials the sibling service "elasticsearch"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_ELASTICSEARCH is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### elasticsearch

- templates: invalid template: application "elasticsearch": env key "discovery.type" invalid

### emqx-enterprise

- service "emqx" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### ente-photos-with-s3

- service "minio" overrides the image command; templates configure through environment only
- service "minio-init" overrides the image entrypoint; templates configure through environment only
- service "minio-init" is a one-shot job (restart: on-failure); templates create long-running applications
- service "museum" waits on "minio"; templates order databases before applications and nothing else
- service "minio-init" waits on "minio"; templates order databases before applications and nothing else
- services "museum" and "web" both publish a public URL; a template routes at most one
- service "museum" reads SERVICE_REALBASE64_ENCRYPTION, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "museum" reads SERVICE_REALBASE64_64_HASH, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "museum" reads SERVICE_REALBASE64_JWT, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "web" names no port and its image exposes none unambiguously; the schema requires one per application
- service "minio" names no port and its image exposes none unambiguously; the schema requires one per application
- service "minio-init" names no port and its image exposes none unambiguously; the schema requires one per application
- generated value SERVICE_PASSWORD_MINIO is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MINIO is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### ente-photos

- services "museum" and "web" both publish a public URL; a template routes at most one
- service "museum" reads SERVICE_REALBASE64_ENCRYPTION, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "museum" reads SERVICE_REALBASE64_64_HASH, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "museum" reads SERVICE_REALBASE64_JWT, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "web" names no port and its image exposes none unambiguously; the schema requires one per application

### esphome

- service "esphome" sets a custom network mode
- service "esphome" runs privileged
- service "esphome" mounts "/etc/localtime:/etc/localtime:ro" from the host; templates declare named volumes only

### espocrm

- service "espocrm-daemon" overrides the image entrypoint; templates configure through environment only
- service "espocrm-daemon" mounts another container's volumes
- service "espocrm-websocket" overrides the image entrypoint; templates configure through environment only
- service "espocrm-websocket" mounts another container's volumes
- service "espocrm-daemon" waits on "espocrm"; templates order databases before applications and nothing else
- service "espocrm-websocket" waits on "espocrm"; templates order databases before applications and nothing else
- services "espocrm" and "espocrm-websocket" both publish a public URL; a template routes at most one

### faraday

- service "faraday" overrides the image entrypoint; templates configure through environment only
- no application is given the cache database's password, which managed databases always require

### firefly

- service "cron" overrides the image entrypoint; templates configure through environment only
- service "cron" names no port and its image exposes none unambiguously; the schema requires one per application
- generated value SERVICE_BASE64_CRONTOKEN is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### flowise-with-databases

- service "flowise" waits on "qdrant"; templates order databases before applications and nothing else
- services "flowise" and "qdrant" both publish a public URL; a template routes at most one
- service "qdrant" names no port and its image exposes none unambiguously; the schema requires one per application
- no application is given the db database's password, which managed databases always require
- no application is given the cache database's password, which managed databases always require

### forgejo-with-mariadb

- service "forgejo" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### forgejo-with-mysql

- service "forgejo" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### forgejo-with-postgresql

- service "forgejo" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### forgejo-with-runner-with-mariadb

- marked `# ignore: true` upstream — Coolify itself does not list it

### forgejo-with-runner-with-mysql

- marked `# ignore: true` upstream — Coolify itself does not list it

### forgejo-with-runner-with-postgresql

- marked `# ignore: true` upstream — Coolify itself does not list it

### forgejo-with-runner

- marked `# ignore: true` upstream — Coolify itself does not list it

### forgejo

- service "forgejo" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### formbricks

- service "minio" overrides the image command; templates configure through environment only
- service "minio-init" overrides the image entrypoint; templates configure through environment only
- service "minio-init" is a one-shot job (restart: no); templates create long-running applications
- service "formbricks" waits on "postgresql"; templates order databases before applications and nothing else
- service "formbricks" waits on "minio"; templates order databases before applications and nothing else
- service "minio-init" waits on "minio"; templates order databases before applications and nothing else
- service "minio" names no port and its image exposes none unambiguously; the schema requires one per application
- service "minio-init" names no port and its image exposes none unambiguously; the schema requires one per application
- DATABASE_URL dials the sibling service "postgresql"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_MINIO is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_POSTGRESQL is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MINIO is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_POSTGRESQL is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### getoutline

- image docker.getoutline.com/outlinewiki/outline:latest could not be pinned to an immutable reference: registry answered 401 Unauthorized

### gitea-runner

- service "runner" overrides the working directory
- no service declares a public port; there is nothing to route the install-time domain at
- service "runner" names no port and its image exposes none unambiguously; the schema requires one per application
- service "runner" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only

### gitea-with-mariadb

- service "gitea" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### gitea-with-mysql

- service "gitea" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### gitea-with-postgresql

- service "gitea" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### gitea

- service "gitea" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### github-runner

- service "runner" sets container security options
- no service declares a public port; there is nothing to route the install-time domain at
- service "runner" names no port and its image exposes none unambiguously; the schema requires one per application
- service "runner" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only

### gitlab

- service "gitlab" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- service "gitlab" sizes shared memory
- GITLAB_OMNIBUS_CONFIG interpolates a variable with no default into "external_url \"${SERVICE_URL_GITLAB}\"; nginx[\"listen_https\"] = false; nginx[\"listen_port\"] = 80; gitlab_rails[\"gitlab_shell_ssh_port\"] = 2222; gitlab_rails[\"smtp_enable\"] = ${GITLAB_SMTP_ENABLE}; gitlab_rails[\"smtp_address\"] = \"${GITLAB_SMTP_ADDRESS}\"; gitlab_rails[\"smtp_port\"] = ${GITLAB_SMTP_PORT}; gitlab_rails[\"smtp_user_name\"] = \"${GITLAB_SMTP_USER_NAME}\"; gitlab_rails[\"smtp_password\"] = \"${GITLAB_SMTP_PASSWORD}\"; gitlab_rails[\"smtp_domain\"] = \"${GITLAB_SMTP_DOMAIN}\"; gitlab_rails[\"smtp_authentication\"] = \"login\"; gitlab_rails[\"smtp_enable_starttls_auto\"] = ${GITLAB_STARTTLS_AUTO}; gitlab_rails[\"smtp_tls\"] = ${GITLAB_SMTP_TLS}; gitlab_rails[\"gitlab_email_from\"] = \"${GITLAB_EMAIL_FROM}\"; gitlab_rails[\"gitlab_email_reply_to\"] = \"${GITLAB_EMAIL_REPLY_TO}\";"; the installed value would be malformed

### glance

- service "glance" mounts "./config:/app/config" from the host; templates declare named volumes only

### glances

- service "glances" shares a host or container PID namespace
- service "glances" runs privileged
- service "glances" mounts "/var/run/docker.sock:/var/run/docker.sock:ro" from the host; templates declare named volumes only
- service "glances" mounts "/run/user/1000/podman/podman.sock:/run/user/1000/podman/podman.sock:ro" from the host; templates declare named volumes only

### glitchtip

- service "worker" overrides the image command; templates configure through environment only
- service "migrate" overrides the image command; templates configure through environment only
- service "migrate" is a one-shot job (restart: no); templates create long-running applications
- no application is given the cache database's password, which managed databases always require
- generated value SERVICE_BASE64_64_ENCRYPTION is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### gotenberg

- service "gotenberg" overrides the image command; templates configure through environment only

### gowa

- generated value SERVICE_PASSWORD_GOWA is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_GOWA is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### grafana

- a hand-written catalog entry already claims this slug; the curated one is kept

### gramps-web

- service "grampsweb_celery" overrides the image command; templates configure through environment only

### grist

- service "grist" reads SERVICE_REALBASE64_128, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it

### hatchet

- service "hatchet-dashboard" overrides the image command; templates configure through environment only
- service "hatchet-engine" overrides the image command; templates configure through environment only
- service "hatchet-engine" is a one-shot job (restart: on-failure); templates create long-running applications
- service "rabbitmq" needs a fixed hostname
- service "migration" overrides the image command; templates configure through environment only
- service "migration" is a one-shot job (restart: no); templates create long-running applications
- service "setup-config" overrides the image command; templates configure through environment only
- service "setup-config" is a one-shot job (restart: no); templates create long-running applications
- service "hatchet-dashboard" waits on "rabbitmq"; templates order databases before applications and nothing else
- service "hatchet-dashboard" waits on "setup-config"; templates order databases before applications and nothing else
- service "hatchet-dashboard" waits on "migration"; templates order databases before applications and nothing else
- service "hatchet-engine" waits on "rabbitmq"; templates order databases before applications and nothing else
- service "hatchet-engine" waits on "setup-config"; templates order databases before applications and nothing else
- service "hatchet-engine" waits on "migration"; templates order databases before applications and nothing else
- service "setup-config" waits on "migration"; templates order databases before applications and nothing else
- service "setup-config" waits on "rabbitmq"; templates order databases before applications and nothing else
- services "hatchet-dashboard" and "rabbitmq" both publish a public URL; a template routes at most one
- service "rabbitmq" names no port and its image exposes none unambiguously; the schema requires one per application
- SERVER_INTERNAL_CLIENT_INTERNAL_GRPC_BROADCAST_ADDRESS dials the sibling service "hatchet-engine"; application containers are named per revision and have no stable address
- SERVER_TASKQUEUE_RABBITMQ_URL dials the sibling service "rabbitmq"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_RABBITMQ is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_RABBITMQ is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### healthchecks

- SECRET_KEY interpolates a variable with no default into "${SECRET_KEY:?${SERVICE_PASSWORD_64_HEALTHCHECKS}}"; the installed value would be malformed

### hermes-agent-with-webui

- service "hermes-agent" overrides the image command; templates configure through environment only
- service "hermes-webui" waits on "hermes-agent"; templates order databases before applications and nothing else
- service "hermes-agent" names no port and its image exposes none unambiguously; the schema requires one per application

### heyform

- service "keydb" overrides the image command; templates configure through environment only
- service "heyform" waits on "mongo"; templates order databases before applications and nothing else
- service "heyform" waits on "keydb"; templates order databases before applications and nothing else
- MONGO_URI dials the sibling service "mongo"; application containers are named per revision and have no stable address
- REDIS_HOST dials the sibling service "keydb"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_KEYDB is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### homarr

- service "homarr" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only
- service "homarr" mounts "./homarr/appdata:/appdata" from the host; templates declare named volumes only

### home-assistant

- service "homeassistant" runs privileged
- service "homeassistant" mounts "/run/dbus:/run/dbus:ro" from the host; templates declare named volumes only

### homepage

- service "homepage" mounts "/var/run/docker.sock:/var/run/docker.sock:ro" from the host; templates declare named volumes only
- service "homepage" mounts "./images:/app/public/images" from the host; templates declare named volumes only

### hoppscotch

- service "db-migration" overrides the image command; templates configure through environment only
- service "db-migration" is a one-shot job (restart: on-failure); templates create long-running applications
- service "backend" waits on "db-migration"; templates order databases before applications and nothing else

### huly

- marked `# ignore: true` upstream — Coolify itself does not list it

### immich

- service "immich" waits on "database"; templates order databases before applications and nothing else
- service "immich" mounts "/etc/localtime:/etc/localtime:ro" from the host; templates declare named volumes only
- service "immich-machine-learning" names no port and its image exposes none unambiguously; the schema requires one per application
- DB_DATABASE_NAME dials the sibling service "immich"; application containers are named per revision and have no stable address
- POSTGRES_DB dials the sibling service "immich"; application containers are named per revision and have no stable address
- no application is given the cache database's password, which managed databases always require
- generated value SERVICE_PASSWORD_POSTGRES is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_POSTGRES is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### infisical

- service "backend" reads SERVICE_REALBASE64_64_AUTHSECRET, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it

### inngest

- service "inngest" overrides the image command; templates configure through environment only
- INNGEST_POSTGRES_URI still names the application database "inngest", which a managed database does not create

### invoice-ninja

- service "invoice-ninja" reads SERVICE_REALBASE64_INVOICENINJA, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it

### jenkins

- service "jenkins" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only

### jitsi

- declares custom networks; every resource joins its environment network
- service "jitsi-web" joins a custom network
- service "prosody" joins a custom network
- service "jicofo" joins a custom network
- service "jvb" joins a custom network
- service "jvb" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- service "jitsi-web" waits on "prosody"; templates order databases before applications and nothing else
- service "jitsi-web" waits on "jicofo"; templates order databases before applications and nothing else
- service "jitsi-web" waits on "jvb"; templates order databases before applications and nothing else
- service "jicofo" waits on "prosody"; templates order databases before applications and nothing else
- service "jvb" waits on "prosody"; templates order databases before applications and nothing else
- service "prosody" names no port and its image exposes none unambiguously; the schema requires one per application
- service "jicofo" names no port and its image exposes none unambiguously; the schema requires one per application
- service "jvb" names no port and its image exposes none unambiguously; the schema requires one per application
- XMPP_BOSH_URL_BASE dials the sibling service "prosody"; application containers are named per revision and have no stable address
- XMPP_SERVER dials the sibling service "prosody"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_JICOFO is used 6 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_JVB is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### joplin

- service "joplin" pins a CPU architecture

### jupyter-notebook-python

- service "jupyter-notebook-python" overrides the image command; templates configure through environment only
- service "jupyter-notebook-python" mounts "/jupyter:/home/jovyan/work" from the host; templates declare named volumes only

### karakeep

- service "chrome" overrides the image command; templates configure through environment only
- service "chrome" names no port and its image exposes none unambiguously; the schema requires one per application
- BROWSER_WEB_URL dials the sibling service "chrome"; application containers are named per revision and have no stable address
- MEILI_ADDR dials the sibling service "meilisearch"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_KARAKEEPNEXTAUTH is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_MEILI is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### keycloak-with-postgres

- service "keycloak" overrides the image command; templates configure through environment only

### keycloak

- service "keycloak" overrides the image command; templates configure through environment only

### kimai

- TRUSTED_HOSTS interpolates a variable with no default into "${TRUSTED_HOSTS}|localhost|127.0.0.1"; the installed value would be malformed

### kuzzle

- service "elasticsearch" sets resource ulimits
- service "kuzzle" adds Linux capabilities
- service "kuzzle" sets kernel parameters
- service "kuzzle" sets resource ulimits
- service "kuzzle" waits on "elasticsearch"; templates order databases before applications and nothing else
- service "elasticsearch" names no port and its image exposes none unambiguously; the schema requires one per application
- DEBUG_SHOW_HIDDEN interpolates a variable with no default into "{$DEBUG_SHOW_HIDDEN:-on}"; the installed value would be malformed
- kuzzle_services__storageEngine__client__node dials the sibling service "elasticsearch"; application containers are named per revision and have no stable address
- no application is given the cache database's password, which managed databases always require

### langfuse

- service "clickhouse" runs as a fixed uid
- service "langfuse" waits on "clickhouse"; templates order databases before applications and nothing else
- service "langfuse" routes on the sub-path "${SERVICE_URL_LANGFUSE_3000}"; templates route a whole domain
- service "clickhouse" names no port and its image exposes none unambiguously; the schema requires one per application
- no application is given the db database's password, which managed databases always require
- no application is given the cache database's password, which managed databases always require

### leantime

- no application is given the cache database's password, which managed databases always require

### librechat

- service "librechat" waits on "meilisearch"; templates order databases before applications and nothing else
- service "librechat" waits on "vectordb"; templates order databases before applications and nothing else
- service "librechat" waits on "rag-api"; templates order databases before applications and nothing else
- service "rag-api" waits on "vectordb"; templates order databases before applications and nothing else
- service "rag-api" names no port and its image exposes none unambiguously; the schema requires one per application
- MEILI_HOST dials the sibling service "meilisearch"; application containers are named per revision and have no stable address
- RAG_API_URL dials the sibling service "rag-api"; application containers are named per revision and have no stable address
- DB_HOST dials the sibling service "vectordb"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_MEILI is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_POSTGRES is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_POSTGRES is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### limesurvey

- no application is given the cache database's password, which managed databases always require

### listmonk

- service "listmonk-initial-database-setup" overrides the image command; templates configure through environment only
- service "listmonk-initial-database-setup" is a one-shot job (restart: no); templates create long-running applications

### litellm

- service "litellm" overrides the image command; templates configure through environment only
- no application is given the cache database's password, which managed databases always require

### litequeen

- marked `# ignore: true` upstream — Coolify itself does not list it

### logto

- service "logto" overrides the image entrypoint; templates configure through environment only

### macos

- marked `# ignore: true` upstream — Coolify itself does not list it

### mage-ai

- service "mage-ai" overrides the image command; templates configure through environment only

### mailpit

- service "mailpit" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### martin

- service "postgresql" pins a CPU architecture
- service "martin" waits on "postgresql"; templates order databases before applications and nothing else
- DATABASE_URL dials the sibling service "postgresql"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_POSTGRES is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_POSTGRES is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### matrix-synapse-with-postgresql

- service "synapse" overrides the image entrypoint; templates configure through environment only

### matrix-synapse-with-sqlite

- service "synapse" overrides the image command; templates configure through environment only
- service "synapse" overrides the image entrypoint; templates configure through environment only

### mattermost

- service "mattermost" pins a CPU architecture

### mautic5

- marked `# ignore: true` upstream — Coolify itself does not list it

### maybe

- marked `# ignore: true` upstream — Coolify itself does not list it

### mealie

- image ghcr.io/mealie-recipes/mealie:3.17.0 could not be pinned to an immutable reference: registry answered 404 Not Found

### metabase

- service "metabase" mounts "/dev/urandom:/dev/random:ro" from the host; templates declare named volumes only

### mindsdb

- service "mindsdb" routes on the sub-path "/api"; templates route a whole domain

### minecraft

- service "mc" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- service "mc" mounts "./minecraft-data:/data" from the host; templates declare named volumes only

### minio-community-edition

- marked `# ignore: true` upstream — Coolify itself does not list it

### minio

- marked `# ignore: true` upstream — Coolify itself does not list it

### mixpost

- service "mixpost" waits on "mysql"; templates order databases before applications and nothing else
- service "mysql" names no port and its image exposes none unambiguously; the schema requires one per application
- no application is given the cache database's password, which managed databases always require
- generated value SERVICE_PASSWORD_MYSQL is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MYSQL is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### mosquitto

- service "mosquitto" overrides the image entrypoint; templates configure through environment only
- service "mosquitto" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- service "mosquitto" mounts "./mosquitto/config:/mosquitto/config" from the host; templates declare named volumes only
- service "mosquitto" mounts "./certs:/certs" from the host; templates declare named volumes only

### n8n-with-postgres-and-worker

- service "n8n-worker" overrides the image command; templates configure through environment only
- service "n8n-worker" waits on "n8n"; templates order databases before applications and nothing else
- service "task-runners" waits on "n8n"; templates order databases before applications and nothing else
- N8N_RUNNERS_TASK_BROKER_URI dials the sibling service "n8n-worker"; application containers are named per revision and have no stable address
- no application is given the cache database's password, which managed databases always require
- generated value SERVICE_PASSWORD_ENCRYPTION is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_N8N is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### n8n-with-postgresql

- service "task-runners" waits on "n8n"; templates order databases before applications and nothing else
- N8N_RUNNERS_TASK_BROKER_URI dials the sibling service "n8n"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_N8N is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### n8n

- service "task-runners" waits on "n8n"; templates order databases before applications and nothing else
- N8N_RUNNERS_TASK_BROKER_URI dials the sibling service "n8n"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_N8N is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### navidrome

- service "navidrome" mounts "./data:/data" from the host; templates declare named volumes only
- service "navidrome" mounts "./music:/music:ro" from the host; templates declare named volumes only

### neon-ws-proxy

- no application is given the db database's password, which managed databases always require

### netbird-client

- service "netbird-client" adds Linux capabilities
- service "netbird-client" sets a custom network mode
- no service declares a public port; there is nothing to route the install-time domain at
- service "netbird-client" names no port and its image exposes none unambiguously; the schema requires one per application

### newt-pangolin

- no service declares a public port; there is nothing to route the install-time domain at

### next-image-transformation

- IMGPROXY_URL dials the sibling service "imgproxy"; application containers are named per revision and have no stable address

### nextcloud-with-mariadb

- no application is given the cache database's password, which managed databases always require

### nextcloud-with-mysql

- no application is given the cache database's password, which managed databases always require

### nextcloud-with-postgres

- no application is given the cache database's password, which managed databases always require

### nexus

- service "nexus" pins a CPU architecture

### nodebb

- service "nodebb" overrides the image command; templates configure through environment only
- no application is given the db database's password, which managed databases always require

### ntfy

- service "ntfy" overrides the image command; templates configure through environment only

### observium

- service "observium" pins a CPU architecture
- service "observium" routes on the sub-path "${SERVICE_URL_OBSERVIUM}"; templates route a whole domain

### ollama-with-open-webui

- service "open-webui" waits on "ollama-api"; templates order databases before applications and nothing else
- OLLAMA_BASE_URL dials the sibling service "ollama-api"; application containers are named per revision and have no stable address

### open-archiver

- service "open-archiver" waits on "meilisearch"; templates order databases before applications and nothing else
- MEILI_HOST dials the sibling service "meilisearch"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_MEILISEARCH is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### openclaw

- service "browser" sizes shared memory
- service "openclaw" waits on "browser"; templates order databases before applications and nothing else
- service "browser" names no port and its image exposes none unambiguously; the schema requires one per application
- BROWSER_CDP_URL dials the sibling service "browser"; application containers are named per revision and have no stable address

### openpanel

- service "openpanel-api" overrides the image command; templates configure through environment only
- service "clickhouse" sets resource ulimits
- service "openpanel-dashboard" waits on "openpanel-api"; templates order databases before applications and nothing else
- service "openpanel-dashboard" waits on "openpanel-worker"; templates order databases before applications and nothing else
- service "openpanel-dashboard" waits on "clickhouse"; templates order databases before applications and nothing else
- service "openpanel-api" waits on "clickhouse"; templates order databases before applications and nothing else
- service "openpanel-worker" waits on "openpanel-api"; templates order databases before applications and nothing else
- service "openpanel-worker" waits on "clickhouse"; templates order databases before applications and nothing else
- services "openpanel-dashboard" and "openpanel-api" both publish a public URL; a template routes at most one
- services "openpanel-dashboard" and "openpanel-worker" both publish a public URL; a template routes at most one
- service "clickhouse" names no port and its image exposes none unambiguously; the schema requires one per application
- CLICKHOUSE_URL dials the sibling service "clickhouse"; application containers are named per revision and have no stable address

### opnform

- service "api-worker" overrides the image command; templates configure through environment only
- service "api-scheduler" overrides the image command; templates configure through environment only
- service "opnform-ui" waits on "opnform-api"; templates order databases before applications and nothing else
- service "nginx" waits on "opnform-api"; templates order databases before applications and nothing else
- service "nginx" waits on "opnform-ui"; templates order databases before applications and nothing else
- service "opnform-ui" names no port and its image exposes none unambiguously; the schema requires one per application
- NUXT_PRIVATE_API_BASE dials the sibling service "nginx"; application containers are named per revision and have no stable address
- no application is given the db database's password, which managed databases always require
- no application is given the cache database's password, which managed databases always require

### orangehrm

- service "orangehrm" pins a CPU architecture

### osticket

- image tiredofit/osticket:latest could not be pinned to an immutable reference: docker Hub answered 404 Not Found

### owncloud

- no application is given the cache database's password, which managed databases always require

### palworld

- service "palworld" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- service "palworld" overrides the stop grace period
- no service declares a public port; there is nothing to route the install-time domain at

### paperless

- service "paperless" reads SERVICE_REALBASE64_64_PAPERLESS, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it

### passbolt

- service "passbolt" overrides the image command; templates configure through environment only

### paymenter

- service "paymenter" routes on the sub-path "${SERVICE_URL_PAYMENTER_80}"; templates route a whole domain

### penpot-with-s3

- service "minio" overrides the image command; templates configure through environment only
- service "minio-init" overrides the image entrypoint; templates configure through environment only
- service "minio-init" is a one-shot job (restart: no); templates create long-running applications
- service "frontend" waits on "penpot-backend"; templates order databases before applications and nothing else
- service "frontend" waits on "penpot-exporter"; templates order databases before applications and nothing else
- service "penpot-backend" waits on "minio-init"; templates order databases before applications and nothing else
- service "minio-init" waits on "minio"; templates order databases before applications and nothing else
- services "frontend" and "mailpit" both publish a public URL; a template routes at most one
- service "penpot-backend" names no port and its image exposes none unambiguously; the schema requires one per application
- service "penpot-backend" reads SERVICE_REALBASE64_64_PENPOT, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "penpot-exporter" names no port and its image exposes none unambiguously; the schema requires one per application
- service "minio" names no port and its image exposes none unambiguously; the schema requires one per application
- service "minio-init" names no port and its image exposes none unambiguously; the schema requires one per application
- service "mailpit" names no port and its image exposes none unambiguously; the schema requires one per application
- PENPOT_EXPORTER_URI dials the sibling service "penpot-exporter"; application containers are named per revision and have no stable address
- PENPOT_OBJECTS_STORAGE_S3_ENDPOINT dials the sibling service "minio"; application containers are named per revision and have no stable address
- PENPOT_SMTP_HOST dials the sibling service "mailpit"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_MINIO is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MINIO is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### penpot

- service "frontend" waits on "penpot-backend"; templates order databases before applications and nothing else
- service "frontend" waits on "penpot-exporter"; templates order databases before applications and nothing else
- services "frontend" and "mailpit" both publish a public URL; a template routes at most one
- service "penpot-backend" names no port and its image exposes none unambiguously; the schema requires one per application
- service "penpot-backend" reads SERVICE_REALBASE64_64_PENPOT, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "penpot-exporter" names no port and its image exposes none unambiguously; the schema requires one per application
- service "mailpit" names no port and its image exposes none unambiguously; the schema requires one per application
- PENPOT_EXPORTER_URI dials the sibling service "penpot-exporter"; application containers are named per revision and have no stable address
- PENPOT_SMTP_HOST dials the sibling service "mailpit"; application containers are named per revision and have no stable address

### peppermint

- marked `# ignore: true` upstream — Coolify itself does not list it

### pi-hole

- service "pihole" adds Linux capabilities
- service "pihole" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### pingvinshare-with-clamav

- marked `# ignore: true` upstream — Coolify itself does not list it

### pingvinshare

- marked `# ignore: true` upstream — Coolify itself does not list it

### plane

- service "api" overrides the image command; templates configure through environment only
- service "worker" overrides the image command; templates configure through environment only
- service "beat-worker" overrides the image command; templates configure through environment only
- service "migrator" overrides the image command; templates configure through environment only
- service "migrator" is a one-shot job (restart: no); templates create long-running applications
- service "plane-minio" overrides the image command; templates configure through environment only
- service "proxy" waits on "web"; templates order databases before applications and nothing else
- service "proxy" waits on "api"; templates order databases before applications and nothing else
- service "proxy" waits on "space"; templates order databases before applications and nothing else
- service "proxy" waits on "admin"; templates order databases before applications and nothing else
- service "proxy" waits on "live"; templates order databases before applications and nothing else
- service "web" waits on "api"; templates order databases before applications and nothing else
- service "web" waits on "worker"; templates order databases before applications and nothing else
- service "space" waits on "api"; templates order databases before applications and nothing else
- service "space" waits on "worker"; templates order databases before applications and nothing else
- service "space" waits on "web"; templates order databases before applications and nothing else
- service "admin" waits on "api"; templates order databases before applications and nothing else
- service "admin" waits on "web"; templates order databases before applications and nothing else
- service "live" waits on "api"; templates order databases before applications and nothing else
- service "live" waits on "web"; templates order databases before applications and nothing else
- service "api" waits on "plane-mq"; templates order databases before applications and nothing else
- service "worker" waits on "api"; templates order databases before applications and nothing else
- service "worker" waits on "plane-mq"; templates order databases before applications and nothing else
- service "beat-worker" waits on "api"; templates order databases before applications and nothing else
- service "beat-worker" waits on "plane-mq"; templates order databases before applications and nothing else
- service "plane-mq" names no port and its image exposes none unambiguously; the schema requires one per application
- service "plane-minio" names no port and its image exposes none unambiguously; the schema requires one per application
- no application is given the db database's password, which managed databases always require
- no application is given the cache database's password, which managed databases always require

### plausible

- marked `# ignore: true` upstream — Coolify itself does not list it

### plunk

- service "plunk" overrides the image entrypoint; templates configure through environment only

### portainer

- service "portainer" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only

### posthog

- marked `# ignore: true` upstream — Coolify itself does not list it

### prefect

- service "prefect" overrides the image command; templates configure through environment only
- service "agent" overrides the image entrypoint; templates configure through environment only
- service "agent" waits on "prefect"; templates order databases before applications and nothing else
- service "agent" names no port and its image exposes none unambiguously; the schema requires one per application
- PREFECT_API_URL dials the sibling service "prefect"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_APIKEY is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### pterodactyl-panel

- marked `# ignore: true` upstream — Coolify itself does not list it

### pterodactyl-with-wings

- marked `# ignore: true` upstream — Coolify itself does not list it

### qbittorrent

- service "qbit" names no port and its image exposes none unambiguously; the schema requires one per application

### rabbitmq

- service "rabbitmq" needs a fixed hostname
- service "rabbitmq" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### rallly

- service "rallly" pins a CPU architecture

### reactive-resume

- service "minio" overrides the image command; templates configure through environment only
- service "chrome" pins a CPU architecture
- service "reactive-resume" waits on "minio"; templates order databases before applications and nothing else
- service "reactive-resume" waits on "chrome"; templates order databases before applications and nothing else
- service "minio" names no port and its image exposes none unambiguously; the schema requires one per application
- CHROME_URL dials the sibling service "chrome"; application containers are named per revision and have no stable address
- STORAGE_ENDPOINT dials the sibling service "minio"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_CHROMETOKEN is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_MINIO is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MINIO is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### redlib

- service "redlib" drops Linux capabilities
- service "redlib" sets container security options
- service "redlib" runs as a fixed uid

### rocketchat

- no application is given the db database's password, which managed databases always require

### rustfs

- marked `# ignore: true` upstream — Coolify itself does not list it

### rybbit

- service "rybbit" waits on "rybbit_backend"; templates order databases before applications and nothing else
- service "rybbit_backend" waits on "rybbit_clickhouse"; templates order databases before applications and nothing else
- service "rybbit_clickhouse" names no port and its image exposes none unambiguously; the schema requires one per application
- CLICKHOUSE_HOST dials the sibling service "rybbit_clickhouse"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_CLICKHOUSE is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### satisfactory

- service "satisfactory-server" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- no service declares a public port; there is nothing to route the install-time domain at
- service "satisfactory-server" names no port and its image exposes none unambiguously; the schema requires one per application

### seafile

- service "memcached" overrides the image entrypoint; templates configure through environment only
- service "seafile" waits on "memcached"; templates order databases before applications and nothing else

### seaweedfs

- service "seaweedfs-master" overrides the image entrypoint; templates configure through environment only
- service "seaweedfs-admin" overrides the image command; templates configure through environment only
- service "seaweedfs-admin" waits on "seaweedfs-master"; templates order databases before applications and nothing else
- services "seaweedfs-master" and "seaweedfs-admin" both publish a public URL; a template routes at most one
- service "seaweedfs-admin" names no port and its image exposes none unambiguously; the schema requires one per application

### sequin

- service "sequin" reads SERVICE_REALBASE64_64_SECRETKEY, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "sequin" reads SERVICE_REALBASE64_VAULTKEY, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it

### sftpgo

- service "sftpgo" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### shlink

- services "shlink" and "shlink-web" both publish a public URL; a template routes at most one
- generated value SERVICE_BASE64_SHLINKAPIKEY is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### signoz

- service "init-clickhouse" overrides the image command; templates configure through environment only
- service "init-clickhouse" is a one-shot job (restart: on-failure); templates create long-running applications
- service "zookeeper" runs as a fixed uid
- service "clickhouse" needs a TTY
- service "clickhouse" sets resource ulimits
- service "signoz" overrides the image command; templates configure through environment only
- service "otel-collector" overrides the image command; templates configure through environment only
- service "schema-migrator-sync" overrides the image command; templates configure through environment only
- service "schema-migrator-sync" is a one-shot job (restart: on-failure); templates create long-running applications
- service "schema-migrator-async" overrides the image command; templates configure through environment only
- service "schema-migrator-async" is a one-shot job (restart: on-failure); templates create long-running applications
- service "clickhouse" waits on "init-clickhouse"; templates order databases before applications and nothing else
- service "clickhouse" waits on "zookeeper"; templates order databases before applications and nothing else
- service "signoz" waits on "clickhouse"; templates order databases before applications and nothing else
- service "signoz" waits on "schema-migrator-sync"; templates order databases before applications and nothing else
- service "otel-collector" waits on "clickhouse"; templates order databases before applications and nothing else
- service "otel-collector" waits on "schema-migrator-sync"; templates order databases before applications and nothing else
- service "otel-collector" waits on "signoz"; templates order databases before applications and nothing else
- service "schema-migrator-sync" waits on "clickhouse"; templates order databases before applications and nothing else
- service "schema-migrator-async" waits on "clickhouse"; templates order databases before applications and nothing else
- service "schema-migrator-async" waits on "schema-migrator-sync"; templates order databases before applications and nothing else
- services "signoz" and "otel-collector" both publish a public URL; a template routes at most one
- service "init-clickhouse" names no port and its image exposes none unambiguously; the schema requires one per application
- service "zookeeper" names no port and its image exposes none unambiguously; the schema requires one per application
- service "clickhouse" names no port and its image exposes none unambiguously; the schema requires one per application
- service "signoz" reads SERVICE_REALBASE64_JWTSECRET, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "otel-collector" names no port and its image exposes none unambiguously; the schema requires one per application
- service "schema-migrator-sync" names no port and its image exposes none unambiguously; the schema requires one per application
- service "schema-migrator-async" names no port and its image exposes none unambiguously; the schema requires one per application
- SIGNOZ_TELEMETRYSTORE_CLICKHOUSE_DSN dials the sibling service "clickhouse"; application containers are named per revision and have no stable address
- STORAGE dials the sibling service "clickhouse"; application containers are named per revision and have no stable address

### soju

- service "soju" joins a custom network
- service "gamja" waits on "soju"; templates order databases before applications and nothing else
- service "soju" names no port and its image exposes none unambiguously; the schema requires one per application

### soketi

- service "soketi" reads SERVICE_REALBASE64_64_SOKETIKEY, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it
- service "soketi" reads SERVICE_REALBASE64_64_SOKETISECRET, which is base64-encoded random bytes — {{secret.N}} cannot reproduce it

### sonarqube

- marked `# ignore: true` upstream — Coolify itself does not list it

### spacebot

- service "spacebot" sets container security options
- service "spacebot" sizes shared memory

### sparkyfitness

- service "sparkyfitness-frontend" waits on "sparkyfitness-server"; templates order databases before applications and nothing else
- SPARKY_FITNESS_SERVER_HOST dials the sibling service "sparkyfitness-server"; application containers are named per revision and have no stable address

### stalwart

- service "stalwart" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- service "stalwart" mounts "/etc/localtime:/etc/localtime:ro" from the host; templates declare named volumes only

### strapi

- service "strapi" overrides the image command; templates configure through environment only
- service "strapi" overrides the image entrypoint; templates configure through environment only
- service "strapi" waits on "postgresql"; templates order databases before applications and nothing else
- DATABASE_HOST dials the sibling service "postgresql"; application containers are named per revision and have no stable address
- POSTGRES_DB dials the sibling service "strapi"; application containers are named per revision and have no stable address
- generated value SERVICE_BASE64_64_SECRET is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_POSTGRESQL is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_POSTGRESQL is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### supabase

- service "supabase-kong" overrides the image entrypoint; templates configure through environment only
- service "supabase-db" overrides the image command; templates configure through environment only
- service "supabase-vector" overrides the image command; templates configure through environment only
- service "supabase-rest" overrides the image command; templates configure through environment only
- service "realtime-dev" overrides the image command; templates configure through environment only
- service "supabase-minio" overrides the image command; templates configure through environment only
- service "minio-createbucket" overrides the image entrypoint; templates configure through environment only
- service "minio-createbucket" is a one-shot job (restart: no); templates create long-running applications
- service "supabase-edge-functions" overrides the image command; templates configure through environment only
- service "supabase-supavisor" overrides the image command; templates configure through environment only
- service "supabase-kong" waits on "supabase-analytics"; templates order databases before applications and nothing else
- service "supabase-studio" waits on "supabase-analytics"; templates order databases before applications and nothing else
- service "supabase-db" waits on "supabase-vector"; templates order databases before applications and nothing else
- service "supabase-analytics" waits on "supabase-db"; templates order databases before applications and nothing else
- service "supabase-rest" waits on "supabase-db"; templates order databases before applications and nothing else
- service "supabase-rest" waits on "supabase-analytics"; templates order databases before applications and nothing else
- service "supabase-auth" waits on "supabase-db"; templates order databases before applications and nothing else
- service "supabase-auth" waits on "supabase-analytics"; templates order databases before applications and nothing else
- service "realtime-dev" waits on "supabase-db"; templates order databases before applications and nothing else
- service "realtime-dev" waits on "supabase-analytics"; templates order databases before applications and nothing else
- service "minio-createbucket" waits on "supabase-minio"; templates order databases before applications and nothing else
- service "supabase-storage" waits on "supabase-db"; templates order databases before applications and nothing else
- service "supabase-storage" waits on "supabase-rest"; templates order databases before applications and nothing else
- service "supabase-storage" waits on "imgproxy"; templates order databases before applications and nothing else
- service "supabase-meta" waits on "supabase-db"; templates order databases before applications and nothing else
- service "supabase-meta" waits on "supabase-analytics"; templates order databases before applications and nothing else
- service "supabase-edge-functions" waits on "supabase-analytics"; templates order databases before applications and nothing else
- service "supabase-supavisor" waits on "supabase-db"; templates order databases before applications and nothing else
- service "supabase-supavisor" waits on "supabase-analytics"; templates order databases before applications and nothing else
- service "supabase-kong" reads SERVICE_SUPABASEANON_KEY, which is a JWT signed with another generated value — {{secret.N}} cannot reproduce it
- service "supabase-kong" reads SERVICE_SUPABASESERVICE_KEY, which is a JWT signed with another generated value — {{secret.N}} cannot reproduce it
- service "supabase-studio" mounts "./volumes/snippets:/app/snippets" from the host; templates declare named volumes only
- service "supabase-studio" mounts "./volumes/functions:/app/edge-functions" from the host; templates declare named volumes only
- service "supabase-studio" reads SERVICE_SUPABASEANON_KEY, which is a JWT signed with another generated value — {{secret.N}} cannot reproduce it
- service "supabase-studio" reads SERVICE_SUPABASESERVICE_KEY, which is a JWT signed with another generated value — {{secret.N}} cannot reproduce it
- service "supabase-analytics" names no port and its image exposes none unambiguously; the schema requires one per application
- service "supabase-vector" names no port and its image exposes none unambiguously; the schema requires one per application
- service "supabase-vector" mounts "/var/run/docker.sock:/var/run/docker.sock:ro" from the host; templates declare named volumes only
- service "supabase-auth" names no port and its image exposes none unambiguously; the schema requires one per application
- service "realtime-dev" names no port and its image exposes none unambiguously; the schema requires one per application
- service "supabase-minio" names no port and its image exposes none unambiguously; the schema requires one per application
- service "supabase-minio" mounts "./volumes/storage:/data" from the host; templates declare named volumes only
- service "minio-createbucket" names no port and its image exposes none unambiguously; the schema requires one per application
- service "supabase-storage" mounts "./volumes/storage:/var/lib/storage" from the host; templates declare named volumes only
- service "supabase-storage" reads SERVICE_SUPABASEANON_KEY, which is a JWT signed with another generated value — {{secret.N}} cannot reproduce it
- service "supabase-storage" reads SERVICE_SUPABASESERVICE_KEY, which is a JWT signed with another generated value — {{secret.N}} cannot reproduce it
- service "imgproxy" mounts "./volumes/storage:/var/lib/storage" from the host; templates declare named volumes only
- service "supabase-edge-functions" names no port and its image exposes none unambiguously; the schema requires one per application
- service "supabase-edge-functions" mounts "./volumes/functions:/home/deno/functions" from the host; templates declare named volumes only
- service "supabase-edge-functions" reads SERVICE_SUPABASEANON_KEY, which is a JWT signed with another generated value — {{secret.N}} cannot reproduce it
- service "supabase-edge-functions" reads SERVICE_SUPABASESERVICE_KEY, which is a JWT signed with another generated value — {{secret.N}} cannot reproduce it
- service "supabase-supavisor" names no port and its image exposes none unambiguously; the schema requires one per application
- LOGFLARE_URL dials the sibling service "supabase-analytics"; application containers are named per revision and have no stable address
- POSTGRES_HOST dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- STUDIO_PG_META_URL dials the sibling service "supabase-meta"; application containers are named per revision and have no stable address
- SUPABASE_URL dials the sibling service "supabase-kong"; application containers are named per revision and have no stable address
- DB_HOSTNAME dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- POSTGRES_BACKEND_URL dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- PGRST_DB_URI dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- API_EXTERNAL_URL dials the sibling service "supabase-kong"; application containers are named per revision and have no stable address
- GOTRUE_DB_DATABASE_URL dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- DB_HOST dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- DATABASE_URL dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- IMGPROXY_URL dials the sibling service "imgproxy"; application containers are named per revision and have no stable address
- POSTGREST_URL dials the sibling service "supabase-rest"; application containers are named per revision and have no stable address
- STORAGE_S3_ENDPOINT dials the sibling service "supabase-minio"; application containers are named per revision and have no stable address
- PG_META_DB_HOST dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- SUPABASE_DB_URL dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- POSTGRES_HOSTNAME dials the sibling service "supabase-db"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_JWT is used 13 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_LOGFLARE is used 4 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_LOGFLAREPRIVATE is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_MINIO is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_PGMETACRYPTO is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_POSTGRES is used 13 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_MINIO is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### supertokens-with-mysql

- image registry.supertokens.io/supertokens/supertokens-mysql:latest could not be pinned to an immutable reference: registry answered 429 Too Many Requests

### sure

- service "worker" overrides the image command; templates configure through environment only
- generated value SERVICE_BASE64_BASE is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### swetrix

- service "clickhouse" adds Linux capabilities
- service "clickhouse" sets resource ulimits
- service "swetrix" waits on "swetrix-api"; templates order databases before applications and nothing else
- service "swetrix-api" waits on "clickhouse"; templates order databases before applications and nothing else
- services "swetrix" and "swetrix-api" both publish a public URL; a template routes at most one
- service "clickhouse" names no port and its image exposes none unambiguously; the schema requires one per application
- CLICKHOUSE_HOST dials the sibling service "clickhouse"; application containers are named per revision and have no stable address
- generated value SERVICE_PASSWORD_CLICKHOUSE is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### syncthing

- service "syncthing" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)

### tailscale-client

- service "tailscale-client" adds Linux capabilities
- service "tailscale-client" maps host devices
- service "tailscale-client" needs a fixed hostname
- service "nginx" sets a custom network mode
- service "nginx" waits on "tailscale-client"; templates order databases before applications and nothing else
- no service declares a public port; there is nothing to route the install-time domain at
- service "tailscale-client" names no port and its image exposes none unambiguously; the schema requires one per application

### teable

- service "teable-db-migrate" is a one-shot job (restart: no); templates create long-running applications
- service "teable-db-migrate" names no port and its image exposes none unambiguously; the schema requires one per application

### termix

- service "termix" waits on "guacd"; templates order databases before applications and nothing else
- GUACD_HOST dials the sibling service "guacd"; application containers are named per revision and have no stable address

### terraria-server

- service "terraria-server" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- service "terraria-server" needs stdin held open
- service "terraria-server" needs a TTY

### trigger

- service "trigger" overrides the image command; templates configure through environment only
- service "trigger" runs as a fixed uid
- service "supervisor" overrides the image command; templates configure through environment only
- service "supervisor" runs as a fixed uid
- service "trigger" waits on "electric"; templates order databases before applications and nothing else
- service "trigger" waits on "clickhouse"; templates order databases before applications and nothing else
- service "supervisor" waits on "trigger"; templates order databases before applications and nothing else
- service "supervisor" waits on "docker-proxy"; templates order databases before applications and nothing else
- service "docker-proxy" mounts "/var/run/docker.sock:/var/run/docker.sock:ro" from the host; templates declare named volumes only
- service "electric" names no port and its image exposes none unambiguously; the schema requires one per application
- service "clickhouse" names no port and its image exposes none unambiguously; the schema requires one per application
- CLICKHOUSE_URL dials the sibling service "clickhouse"; application containers are named per revision and have no stable address
- ELECTRIC_ORIGIN dials the sibling service "electric"; application containers are named per revision and have no stable address
- RUN_REPLICATION_CLICKHOUSE_URL dials the sibling service "clickhouse"; application containers are named per revision and have no stable address
- DOCKER_HOST dials the sibling service "docker-proxy"; application containers are named per revision and have no stable address
- OTEL_EXPORTER_OTLP_ENDPOINT dials the sibling service "trigger"; application containers are named per revision and have no stable address
- TRIGGER_API_URL dials the sibling service "trigger"; application containers are named per revision and have no stable address
- no application is given the cache database's password, which managed databases always require
- generated value SERVICE_PASSWORD_64_CLICKHOUSE is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_PASSWORD_64_WORKERSECRET is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match
- generated value SERVICE_USER_CLICKHOUSE is used 3 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### triliumnext

- service "triliumnext" pins a CPU architecture

### twenty

- service "worker" overrides the image command; templates configure through environment only
- service "worker" waits on "twenty"; templates order databases before applications and nothing else
- services "twenty" and "worker" both publish a public URL; a template routes at most one
- service "worker" names no port and its image exposes none unambiguously; the schema requires one per application
- generated value SERVICE_BASE64_32_SECRET is used 2 times; {{secret.N}} resolves freshly at each use, so the copies would not match

### umami

- a hand-written catalog entry already claims this slug; the curated one is kept

### unleash-without-database

- service "unleash" overrides the image command; templates configure through environment only

### uptime-kuma-with-mariadb

- no application is given the db database's password, which managed databases always require

### uptime-kuma-with-mysql

- no application is given the db database's password, which managed databases always require

### uptime-kuma

- a hand-written catalog entry already claims this slug; the curated one is kept

### vaultwarden

- a hand-written catalog entry already claims this slug; the curated one is kept

### vikunja

- service "init" overrides the image command; templates configure through environment only
- service "init" is a one-shot job (restart: no); templates create long-running applications
- service "vikunja" waits on "init"; templates order databases before applications and nothing else
- service "init" names no port and its image exposes none unambiguously; the schema requires one per application

### weaviate

- service "weaviate" overrides the image command; templates configure through environment only

### windmill

- service "windmill-worker-1" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only
- service "windmill-worker-2" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only
- service "windmill-worker-3" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only

### windows

- marked `# ignore: true` upstream — Coolify itself does not list it

### wings

- service "wings" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- service "wings" mounts "/var/run/docker.sock:/var/run/docker.sock" from the host; templates declare named volumes only
- service "wings" mounts "/var/lib/docker/containers/:/var/lib/docker/containers/" from the host; templates declare named volumes only
- service "wings" mounts "/var/lib/pterodactyl/volumes:/var/lib/pterodactyl/volumes" from the host; templates declare named volumes only
- service "wings" mounts "/tmp/pterodactyl:/tmp/pterodactyl" from the host; templates declare named volumes only

### wireguard-easy

- service "wg-easy" adds Linux capabilities
- service "wg-easy" overrides the image entrypoint; templates configure through environment only
- service "wg-easy" publishes fixed host ports, which cannot be redeployed today (application-deploy.md §5)
- service "wg-easy" sets kernel parameters

### zep

- marked `# ignore: true` upstream — Coolify itself does not list it

## Converted templates

- `actualbudget` — Actualbudget
- `appsmith` — Appsmith
- `audiobookshelf` — Audiobookshelf
- `babybuddy` — Babybuddy
- `bento-pdf` — Bento PDF
- `browserless` — Browserless
- `budge` — Budge
- `bugsink` — Bugsink
- `calibre-web` — Calibre Web
- `cap-captcha` — Cap Captcha
- `cap` — Cap
- `celld` — Celld
- `chroma` — Chroma
- `classicpress-with-mariadb` — Classicpress with MariaDB
- `classicpress-with-mysql` — Classicpress with MySQL
- `classicpress-without-database` — Classicpress Without Database
- `cloudbeaver` — Cloudbeaver
- `cockpit` — Cockpit
- `code-server` — Code Server
- `databasus` — Databasus
- `directus` — Directus
- `docmost` — Docmost
- `docuseal-with-postgres` — Docuseal with PostgreSQL
- `docuseal` — Docuseal
- `dokuwiki` — Dokuwiki
- `dolibarr` — Dolibarr
- `drizzle-gateway` — Drizzle Gateway
- `drupal-with-postgresql` — Drupal with PostgreSQL
- `duplicati` — Duplicati
- `easyappointments` — Easyappointments
- `electricsql` — Electricsql
- `emby` — Emby
- `embystat` — Embystat
- `evolution-api` — Evolution API
- `excalidraw` — Excalidraw
- `fider` — Fider
- `filebrowser` — Filebrowser
- `fileflows` — Fileflows
- `firefox` — Firefox
- `fizzy` — Fizzy
- `flipt` — Flipt
- `flowise` — Flowise
- `foundryvtt` — Foundryvtt
- `freescout` — Freescout
- `freshrss-with-mariadb` — FreshRSS with MariaDB
- `freshrss-with-mysql` — FreshRSS with MySQL
- `freshrss-with-postgresql` — FreshRSS with PostgreSQL
- `freshrss` — FreshRSS
- `garage` — Garage
- `ghost` — Ghost
- `glpi` — Glpi
- `goatcounter` — Goatcounter
- `gotify` — Gotify
- `grafana-with-postgresql` — Grafana with PostgreSQL
- `grimmory` — Grimmory
- `grocy` — Grocy
- `heimdall` — Heimdall
- `homebox` — Homebox
- `imgcompress` — Imgcompress
- `influxdb` — InfluxDB
- `it-tools` — It Tools
- `jean` — Jean
- `jellyfin` — Jellyfin
- `joomla-with-mariadb` — Joomla with MariaDB
- `labelstudio` — Labelstudio
- `langflow` — Langflow
- `libreoffice` — Libreoffice
- `librespeed` — Librespeed
- `libretranslate` — Libretranslate
- `linkding-plus` — Linkding Plus
- `linkding` — Linkding
- `lobe-chat` — Lobe Chat
- `lowcoder` — Lowcoder
- `mediawiki` — Mediawiki
- `meilisearch` — Meilisearch
- `memos` — Memos
- `metamcp` — Metamcp
- `metube` — Metube
- `miniflux` — Miniflux
- `moodle` — Moodle
- `newapi` — Newapi
- `nextcloud` — Nextcloud
- `nexus-arm` — Nexus Arm
- `nitropage-with-postgresql` — Nitropage with PostgreSQL
- `nitropage` — Nitropage
- `nocobase` — Nocobase
- `nocodb` — NocoDB
- `odoo` — Odoo
- `once-campfire` — Once Campfire
- `onedev` — Onedev
- `onetimesecret` — Onetimesecret
- `open-webui` — Open WebUI
- `openobserve` — Openobserve
- `organizr` — Organizr
- `overseerr` — Overseerr
- `pairdrop` — Pairdrop
- `pgadmin` — pgAdmin
- `pgbackweb` — Pgbackweb
- `phpmyadmin` — phpMyAdmin
- `plex` — Plex
- `pocket-id-with-postgresql` — Pocket ID with PostgreSQL
- `pocket-id` — Pocket ID
- `pocketbase` — Pocketbase
- `postiz` — Postiz
- `privatebin` — Privatebin
- `prowlarr` — Prowlarr
- `proxyscotch` — Proxyscotch
- `qdrant` — Qdrant
- `radarr` — Radarr
- `readeck` — Readeck
- `redis-insight` — Redis Insight
- `redmine` — Redmine
- `rivet-engine` — Rivet Engine
- `ryot` — Ryot
- `searxng` — Searxng
- `sessy` — Sessy
- `silverbullet` — Silverbullet
- `siyuan` — Siyuan
- `slash` — Slash
- `snapdrop` — Snapdrop
- `soketi-app-manager` — Soketi App Manager
- `sonarr` — Sonarr
- `statusnook` — Statusnook
- `stirling-pdf` — Stirling PDF
- `superset-with-postgresql` — Superset with PostgreSQL
- `supertokens-with-postgresql` — Supertokens with PostgreSQL
- `tolgee` — Tolgee
- `traccar` — Traccar
- `trailbase` — Trailbase
- `transmission` — Transmission
- `typesense` — Typesense
- `unleash-with-postgresql` — Unleash with PostgreSQL
- `unstructured` — Unstructured
- `usesend` — Usesend
- `vert` — Vert
- `vikunja-with-postgresql` — Vikunja with PostgreSQL
- `vvveb-with-mariadb` — Vvveb with MariaDB
- `vvveb-with-mysql` — Vvveb with MySQL
- `vvveb` — Vvveb
- `wakapi` — Wakapi
- `web-check` — Web Check
- `weblate` — Weblate
- `whoogle` — Whoogle
- `wikijs` — Wikijs
- `wordpress-with-mariadb` — WordPress with MariaDB
- `wordpress-with-mysql` — WordPress with MySQL
- `wordpress-without-database` — WordPress Without Database
- `yamtrack-with-postgresql` — Yamtrack with PostgreSQL
- `yamtrack` — Yamtrack
- `zipline` — Zipline
